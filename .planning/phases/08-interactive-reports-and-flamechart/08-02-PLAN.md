---
phase: 08-interactive-reports-and-flamechart
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/reports/LatencyFlamechart.tsx
  - src/components/reports/TestReports.tsx
  - src/a11y/en.json
autonomous: true
requirements:
  - REPORT-02

must_haves:
  truths:
    - "LatencyFlamechart renders a visual waterfall timeline of step execution durations"
    - "Steps exceeding latency thresholds (> 800ms / 1500ms) are highlighted with slow bottleneck badges"
    - "Users can toggle between step list view and latency flamechart view in TestReports.tsx"
    - "All text strings flow through en.json via useTranslation"
---

<objective>
Build `LatencyFlamechart.tsx` component visualizing execution step duration waterfall and highlight latency bottlenecks, integrating into `TestReports.tsx`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Build LatencyFlamechart component</name>
  <files>src/components/reports/LatencyFlamechart.tsx, src/a11y/en.json</files>
  <action>
    1. Create `src/components/reports/LatencyFlamechart.tsx` rendering step latency bars with duration badges, percentage distribution, and bottleneck warning chips.
    2. Add translations in `src/a11y/en.json` under `reports.flamechart.*`.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Integrate flamechart toggle in TestReports</name>
  <files>src/components/reports/TestReports.tsx</files>
  <action>
    1. In `src/components/reports/TestReports.tsx`, add a view mode toggle (Steps List vs. Latency Flamechart).
    2. Render `LatencyFlamechart` when flamechart view mode is active.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }</automated>
  </verify>
</task>

</tasks>

<output>
Create `.planning/phases/08-interactive-reports-and-flamechart/08-02-SUMMARY.md` on completion.
</output>
