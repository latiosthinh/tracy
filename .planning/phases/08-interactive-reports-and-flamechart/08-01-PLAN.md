---
phase: 08-interactive-reports-and-flamechart
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/htmlReportExporter.ts
  - src/utils/htmlReportExporter.test.ts
  - src/components/reports/TestReports.tsx
  - src/a11y/en.json
autonomous: true
requirements:
  - REPORT-01

must_haves:
  truths:
    - "htmlReportExporter produces a standalone single-file HTML report with zero external dependencies"
    - "HTML report includes execution metadata, passed/failed stats, step timeline with duration, and embedded failure screenshots"
    - "Users can click Download HTML Report in TestReports.tsx to save .html report"
    - "All text strings flow through en.json via useTranslation"
---

<objective>
Build standalone HTML test report bundle generator (`src/utils/htmlReportExporter.ts`), unit test suite, and export triggers in `TestReports.tsx`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build standalone HTML report generator and tests</name>
  <files>src/utils/htmlReportExporter.ts, src/utils/htmlReportExporter.test.ts</files>
  <action>
    1. In `src/utils/htmlReportExporter.ts`, implement `generateStandaloneHtmlReport(result: FlowExecutionResult, flowName: string): string` generating valid HTML5 with embedded CSS styling, status badges, step breakdown table, and collapsible screenshot viewer.
    2. Write unit tests in `src/utils/htmlReportExporter.test.ts` verifying HTML output structure, step counts, metadata, and screenshot embedding.
  </action>
  <verify>
    <automated>pnpm test src/utils/htmlReportExporter.test.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Add Download HTML Report button to TestReports</name>
  <files>src/components/reports/TestReports.tsx, src/a11y/en.json</files>
  <action>
    1. In `src/components/reports/TestReports.tsx`, add a "Download HTML Report" button (`Download` icon) alongside the existing JSON / copy actions.
    2. Add translations in `src/a11y/en.json` under `reports.htmlExport.*`.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }</automated>
  </verify>
</task>

</tasks>

<output>
Create `.planning/phases/08-interactive-reports-and-flamechart/08-01-SUMMARY.md` on completion.
</output>
