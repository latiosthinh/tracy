---
phase: 23-studio-network-matrix-grid-and-profiler-panels
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - src/components/network/NetworkMockInspector.tsx
  - src/components/network/MockRuleEditorModal.tsx
  - src/components/network/RequestWaterfallView.tsx
  - src/components/network/NetworkMockInspector.test.tsx
  - src/components/matrix/MatrixRunnerPanel.tsx
  - src/components/matrix/BrowserWorkerCard.tsx
  - src/components/matrix/MatrixResultsGrid.tsx
  - src/components/matrix/MatrixRunnerPanel.test.tsx
  - src/components/perf/WebVitalsScorecard.tsx
  - src/components/perf/MetricGauge.tsx
  - src/components/perf/PerfProfilerPanel.tsx
  - src/components/perf/WebVitalsScorecard.test.tsx
  - src/components/studio/StudioTabs.tsx
  - src/components/studio/StudioRightSidebar.tsx
  - src/components/studio/StudioView.tsx
  - src/components/reports/FlowReportModal.tsx
  - src/a11y/en.json
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
must_haves:
  truths:
    - "Studio provides an interactive Network Mocking inspector and rule editor to configure, toggle, and inspect route interception rules with live request waterfall (UI-01)"
    - "Multi-Browser Matrix panel displays real-time execution progress, active worker allocation, and per-browser pass/fail cards across Chromium, Firefox, and WebKit (UI-02)"
    - "Core Web Vitals scorecard renders real-time performance gauges with green/amber/red threshold ratings (good/needs-improvement/poor) for monitored journeys (UI-03)"
    - "All new panels integrate into Studio tabs, sidebar switchboards, modals, and test reports with 100% accessible i18n string extraction (UI-01, UI-02, UI-03, UI-04)"
  artifacts:
    - path: "src/components/network/NetworkMockInspector.tsx"
      provides: "Main Network Mock Inspector tab with rule management table, interception switch, and traffic log filter"
      exports: ["NetworkMockInspector"]
    - path: "src/components/network/MockRuleEditorModal.tsx"
      provides: "Modal for creating/editing URL matching rules, status codes, headers, delay, abort reason, and mock response body"
      exports: ["MockRuleEditorModal"]
    - path: "src/components/network/RequestWaterfallView.tsx"
      provides: "Live request waterfall table with status pills, timing bars, mock hit badges, and request/response payload drawer"
      exports: ["RequestWaterfallView"]
    - path: "src/components/matrix/MatrixRunnerPanel.tsx"
      provides: "Multi-Browser execution controller with engine checkboxes (Chromium, Firefox, WebKit), concurrency slider, and run button"
      exports: ["MatrixRunnerPanel"]
    - path: "src/components/matrix/BrowserWorkerCard.tsx"
      provides: "Real-time worker card rendering browser engine icon, step progress bar, active step label, duration, and status badge"
      exports: ["BrowserWorkerCard"]
    - path: "src/components/matrix/MatrixResultsGrid.tsx"
      provides: "Aggregated matrix grid displaying pass/fail comparison across engines and flow steps"
      exports: ["MatrixResultsGrid"]
    - path: "src/components/perf/WebVitalsScorecard.tsx"
      provides: "Core Web Vitals scorecard with color-coded gauge indicators (LCP, CLS, INP, FCP, TTFB)"
      exports: ["WebVitalsScorecard"]
    - path: "src/components/perf/MetricGauge.tsx"
      provides: "Individual circular or radial metric gauge with threshold boundary labels and rating badges"
      exports: ["MetricGauge"]
    - path: "src/components/perf/PerfProfilerPanel.tsx"
      provides: "Main performance profiler panel combining CWV scorecard, throttling preset selector, and budget assertion list"
      exports: ["PerfProfilerPanel"]
  key_links:
    - from: "src/components/network/NetworkMockInspector.tsx"
      to: "src/stores/networkStore.ts"
      via: "useNetworkStore hook for rules, requests, interception toggle"
      pattern: "useNetworkStore"
    - from: "src/components/matrix/MatrixRunnerPanel.tsx"
      to: "src/stores/matrixStore.ts"
      via: "useMatrixStore hook for browser selection, execution triggers, worker state"
      pattern: "useMatrixStore"
    - from: "src/components/perf/PerfProfilerPanel.tsx"
      to: "src/stores/perfStore.ts"
      via: "usePerfStore hook for metrics, ratings, throttling controls"
      pattern: "usePerfStore"
    - from: "src/components/studio/StudioTabs.tsx"
      to: "src/components/studio/StudioRightSidebar.tsx"
      via: "Tab switchboard rendering network, matrix, and perf tabs"
      pattern: "network|matrix|perf"
---

<objective>
Build the visual Studio components for Network Mocking Inspector & Rule Editor, Multi-Browser Matrix Runner panel, and Core Web Vitals Profiler Scorecard. Integrate all panels into Studio tabs, right sidebars, modals, and test reports with 100% accessible i18n extraction.

Purpose: Deliver intuitive, reactive Studio diagnostics for network interception, cross-browser parallel testing, and Core Web Vitals benchmarking.
Output: Network, Matrix, and Perf component suites in `src/components/`, Studio tab integration, test suites, and updated `src/a11y/en.json`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/23-studio-network-matrix-grid-and-profiler-panels/23-01-PLAN.md
@src/types/network.ts
@src/types/matrix.ts
@src/types/perf.ts
@src/stores/networkStore.ts
@src/stores/matrixStore.ts
@src/stores/perfStore.ts
@src/components/studio/StudioTabs.tsx
@src/components/studio/StudioRightSidebar.tsx
@src/components/studio/StudioView.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Network Mock Inspector & Rule Editor with Live Request Waterfall (UI-01)</name>
  <files>src/components/network/NetworkMockInspector.tsx, src/components/network/MockRuleEditorModal.tsx, src/components/network/RequestWaterfallView.tsx, src/components/network/NetworkMockInspector.test.tsx</files>
  <behavior>
    - Test 1: NetworkMockInspector renders active interception toggle, Add Mock Rule button, rule table, and search/method filters.
    - Test 2: MockRuleEditorModal validates URL pattern (glob/regex/exact), method, status code (100-599), headers, and formatted JSON/text response body.
    - Test 3: RequestWaterfallView lists captured HTTP requests with status pills (2xx green, 4xx/5xx red, mocked purple), duration timing bars, and side drawer inspecting request/response headers and payloads.
    - Test 4: Toggling rule enable/disable or deleting rule updates state and dispatches IPC sync to Electron.
  </behavior>
  <action>
1. Create `src/components/network/MockRuleEditorModal.tsx`:
   - Form for creating/editing `NetworkMockRule`:
     - Rule Name input.
     - Pattern Type selector (`exact`, `glob`, `regex`) and URL Pattern input with placeholder examples (`/api/v1/users/*`, `^https://api\\.acme\\.com/v2/.*`).
     - HTTP Method dropdown (`ALL`, `GET`, `POST`, `PUT`, `DELETE`, `PATCH`).
     - Action type radio: `Fulfill Response` vs `Abort Request`.
     - Fulfill fields: Status Code (default 200), Content-Type dropdown/input, Response Headers key-value editor, Response Body textarea with JSON validation helper, Delay (ms) input.
     - Abort fields: Abort reason dropdown (`failed`, `timedout`, `connectionreset`, `accessdenied`, `blockedbyclient`).
     - Save and Cancel buttons with full a11y labels.
2. Create `src/components/network/RequestWaterfallView.tsx`:
   - Split-pane layout:
     - Left: Request table with columns for Status, Method badge, URL path, Type/MIME, Duration (with mini horizontal bar chart), and Mocked badge (purple pill). Search input and method filter chips.
     - Right: Selected request detail inspector showing Request Headers, Response Headers, Query Parameters, and formatted Request/Response Payloads (with copy button and JSON collapsible view).
     - Clear logs button and Export HAR button.
3. Create `src/components/network/NetworkMockInspector.tsx`:
   - Main container tab:
     - Header: Global Interception Master Switch ("Enable Network Mocking"), Rule Count badge, "Add Mock Rule" button, "Import HAR" button.
     - Upper pane: Active Mock Rules table with Enable/Disable switches, Pattern, Method, Status, Delay, Edit, and Delete actions.
     - Lower pane: Embedded `RequestWaterfallView` showing live traffic.
4. Write unit and component tests in `NetworkMockInspector.test.tsx`.
  </action>
  <verify>
    <automated>pnpm test src/components/network/NetworkMockInspector.test.tsx</automated>
  </verify>
  <done>Network Mock Inspector and Rule Editor components built with full interaction and test coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build Multi-Browser Matrix Runner Panel & Parallel Progress Cards (UI-02)</name>
  <files>src/components/matrix/MatrixRunnerPanel.tsx, src/components/matrix/BrowserWorkerCard.tsx, src/components/matrix/MatrixResultsGrid.tsx, src/components/matrix/MatrixRunnerPanel.test.tsx</files>
  <behavior>
    - Test 1: MatrixRunnerPanel allows selecting browser targets (Chromium, Firefox, WebKit) and configuring worker concurrency.
    - Test 2: Clicking "Run Multi-Browser Matrix" launches parallel execution across selected browsers.
    - Test 3: BrowserWorkerCard displays real-time execution state for each engine: active step index, step name, animated progress bar, duration counter, and status badges.
    - Test 4: MatrixResultsGrid renders comprehensive step-by-step cross-browser matrix comparison table highlighting passed, failed, and skipped steps per engine.
  </behavior>
  <action>
1. Create `src/components/matrix/BrowserWorkerCard.tsx`:
   - Component rendering individual browser worker status:
     - Header: Engine icon & name (Chromium blue, Firefox orange, WebKit purple), Status badge (`idle`, `running`, `passed`, `failed`).
     - Live progress: Progress bar (`currentStep / totalSteps * 100`), current step label with pulsing spinner if active.
     - Metrics: Total duration (`${durationMs}ms`), Passed steps count (green), Failed steps count (red), Skipped steps count (slate).
     - Error banner if worker encountered execution failure with expandable stack trace.
2. Create `src/components/matrix/MatrixResultsGrid.tsx`:
   - Matrix comparison table:
     - Rows: Flow test steps in order.
     - Columns: Step Name, Chromium, Firefox, WebKit.
     - Cell values: Pass checkmark (emerald), Fail cross (rose), Skip hyphen (amber/slate) with execution duration.
     - Footer summary: Total flow time, Engine pass rates, and Download JUnit XML / Matrix JSON export buttons.
3. Create `src/components/matrix/MatrixRunnerPanel.tsx`:
   - Top Control Bar:
     - Browser Engine Checkboxes (`Chromium`, `Firefox`, `WebKit`) with "Select All" toggle.
     - Concurrency slider / number input (1 to CPU cores, default 2).
     - "Run Matrix Suite" action button with split-engine icon and loading state.
     - "Cancel Matrix" button when running.
   - Main View:
     - Live execution grid: 3-column responsive layout rendering `BrowserWorkerCard` for each active engine.
     - Completed matrix summary: Aggregated `MatrixResultsGrid` when run is finished or historical runs exist.
4. Write unit tests in `MatrixRunnerPanel.test.tsx`.
  </action>
  <verify>
    <automated>pnpm test src/components/matrix/MatrixRunnerPanel.test.tsx</automated>
  </verify>
  <done>Multi-Browser Matrix Runner panel with worker cards and aggregated results grid passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build Core Web Vitals Scorecard, Gauges, and Studio Tab Integration (UI-03, UI-04)</name>
  <files>src/components/perf/WebVitalsScorecard.tsx, src/components/perf/MetricGauge.tsx, src/components/perf/PerfProfilerPanel.tsx, src/components/perf/WebVitalsScorecard.test.tsx, src/components/studio/StudioTabs.tsx, src/components/studio/StudioRightSidebar.tsx, src/components/studio/StudioView.tsx, src/components/reports/FlowReportModal.tsx, src/a11y/en.json</files>
  <behavior>
    - Test 1: MetricGauge displays numerical value, unit, and color-coded status badge based on rating ('good' green, 'needs-improvement' amber, 'poor' red).
    - Test 2: WebVitalsScorecard renders gauges for all 5 Core Web Vitals (LCP, CLS, INP, FCP, TTFB) with official threshold guidelines.
    - Test 3: PerfProfilerPanel includes Throttling preset selector ('none', 'slow3g', 'fast3g', 'offline', CPU 4x/6x slowdown) and list of evaluated performance assertions.
    - Test 4: StudioTabs and StudioRightSidebar render Network, Matrix, and Perf tabs seamlessly switching active views.
    - Test 5: FlowReportModal embeds WebVitalsScorecard when performance telemetry is present in test results.
    - Test 6: 100% of user-facing strings are localized in src/a11y/en.json with zero hardcoded English literals.
  </behavior>
  <action>
1. Create `src/components/perf/MetricGauge.tsx`:
   - Circular SVG or card gauge rendering:
     - Metric Name (e.g. "LCP - Largest Contentful Paint").
     - Formatted Value (e.g. "1,840 ms", "0.04").
     - Rating Pill: `Good` (emerald-500), `Needs Improvement` (amber-500), `Poor` (rose-500).
     - Threshold scale visualization (showing <2.5s / 2.5-4.0s / >4.0s brackets).
     - Tooltip explaining metric meaning and user experience impact.
2. Create `src/components/perf/WebVitalsScorecard.tsx`:
   - Grid layout of `MetricGauge` components for:
     - LCP (Largest Contentful Paint)
     - CLS (Cumulative Layout Shift)
     - INP (Interaction to Next Paint)
     - FCP (First Contentful Paint)
     - TTFB (Time to First Byte)
   - Secondary metrics: DOMContentLoaded, Window Load, JS Heap Size (Chromium).
3. Create `src/components/perf/PerfProfilerPanel.tsx`:
   - Throttling Toolbar:
     - Network Throttling preset dropdown: `No Throttling`, `Fast 3G`, `Slow 3G`, `Offline`.
     - CPU Throttling dropdown: `1x (No throttling)`, `4x Slowdown`, `6x Slowdown`.
     - "Apply to Live Browser" button triggering `tracyApi.applyThrottling()`.
   - Scorecard view: `<WebVitalsScorecard />`.
   - Performance Assertions Section: Table listing flow `assert_performance` assertions with Expected Budget vs Actual Measured and Pass/Fail status pills.
4. Integrate panels into Studio:
   - Update `src/components/studio/StudioTabs.tsx`:
     - Add `{ id: 'network', labelKey: 'studio.tabNetwork', icon: Globe }`.
     - Add `{ id: 'matrix', labelKey: 'studio.tabMatrix', icon: Grid }`.
     - Add `{ id: 'perf', labelKey: 'studio.tabPerf', icon: Gauge }`.
   - Update `src/components/studio/StudioRightSidebar.tsx` / `StudioView.tsx`:
     - Wire `'network'` tab -> `<NetworkMockInspector />`.
     - Wire `'matrix'` tab -> `<MatrixRunnerPanel />`.
     - Wire `'perf'` tab -> `<PerfProfilerPanel />`.
   - Update `src/components/reports/FlowReportModal.tsx`:
     - Embed `<WebVitalsScorecard />` in the test run report when `flowResult.metrics` exists.
5. Extract all newly added strings into `src/a11y/en.json` under `"network"`, `"matrix"`, `"perf"`, and `"studio"` keys.
6. Write unit tests for `WebVitalsScorecard.test.tsx` and run full lint/test suites.
  </action>
  <verify>
    <automated>pnpm lint && pnpm test</automated>
  </verify>
  <done>Core Web Vitals scorecard, Matrix Runner, and Network Mocking panels fully integrated into Studio with zero lint errors and all unit tests passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Input -> Mock Rules | User enters regex patterns and JSON bodies in MockRuleEditorModal. |
| User Input -> Throttling CDP Session | User selects CPU/Network throttling presets affecting embedded Chromium instance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Denial of Service (ReDoS) | `MockRuleEditorModal.tsx` | mitigate | Validate regex patterns with try/catch `new RegExp()` and reject invalid or unbounded expressions before saving. |
| T-23-04 | Process Crash (CDP Non-Chromium) | `PerfProfilerPanel.tsx` | mitigate | Guard CPU/Network throttling controls with active browser engine check; disable CDP-only controls when running Firefox/WebKit. |
</threat_model>

<verification>
- `pnpm lint` passes with 0 warnings and 0 errors
- `pnpm test` passes all component and store unit tests
- `pnpm typecheck` passes cleanly
</verification>

<success_criteria>
Studio Network Mocking inspector with live waterfall, Multi-Browser Matrix Runner panel with parallel worker cards, and Core Web Vitals profiler scorecard with gauge indicators fully implemented, tested, and integrated into Studio tabs and reports.
</success_criteria>

<output>
After completion, create `.planning/phases/23-studio-network-matrix-grid-and-profiler-panels/23-02-SUMMARY.md`
</output>
