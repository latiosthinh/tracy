---
phase: 18-autonomous-route-and-interaction-crawler
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/core/crawler/types.ts
  - electron/core/crawler/domSkeleton.ts
  - electron/core/crawler/domSkeleton.test.ts
  - electron/core/crawler/safetyFilter.ts
  - electron/core/crawler/safetyFilter.test.ts
  - electron/core/crawler/formExplorer.ts
  - electron/core/crawler/formExplorer.test.ts
autonomous: true
requirements:
  - CRAWL-02
  - CRAWL-03
  - CRAWL-04
must_haves:
  truths:
    - "DOM structural skeleton hashing normalizes dynamic text/attributes and identifies unique SPA dynamic views with cycle detection (CRAWL-02)"
    - "Destructive action safety filter blocks dangerous keywords (/logout|delete|remove|destroy|signout|cancel/i) from element interaction (CRAWL-03)"
    - "Safe form exploration detects input types and generates contextual synthetic mock data without submitting destructive endpoints (CRAWL-04)"
  artifacts:
    - path: "electron/core/crawler/types.ts"
      provides: "Crawler contracts, graph nodes, edge actions, discovery state interfaces"
      min_lines: 40
    - path: "electron/core/crawler/domSkeleton.ts"
      provides: "DOM structure simplification and fast hash generator"
      exports: ["computeDomSkeletonHash", "extractStructuralSkeleton"]
    - path: "electron/core/crawler/safetyFilter.ts"
      provides: "Destructive element blocker and safety scoring"
      exports: ["isDestructiveAction", "filterSafeInteractiveElements", "DANGEROUS_ACTION_PATTERN"]
    - path: "electron/core/crawler/formExplorer.ts"
      provides: "Contextual synthetic form mock generator and input action planner"
      exports: ["generateSyntheticFormData", "planFormInteractions"]
  key_links:
    - from: "electron/core/crawler/safetyFilter.ts"
      to: "electron/core/crawler/types.ts"
      via: "Type imports for InteractiveElement"
      pattern: "import.*InteractiveElement.*from.*types"
    - from: "electron/core/crawler/formExplorer.ts"
      to: "electron/core/crawler/safetyFilter.ts"
      via: "Safety verification on form submit buttons"
      pattern: "isDestructiveAction"
---

<objective>
Build core crawler primitives: type definitions, DOM structural skeleton hashing (cycle breaker), destructive action safety filter, and safe synthetic form explorer.

Purpose: Provide deterministic, safe, and cycle-resistant building blocks for the autonomous route crawler.
Output: Core modules in `electron/core/crawler/` with full Vitest test coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Projects/QA/tracy/.planning/PROJECT.md
@D:/Projects/QA/tracy/.planning/REQUIREMENTS.md
@D:/Projects/QA/tracy/.planning/ROADMAP.md
@D:/Projects/QA/tracy/docs/FLOW_SCHEMA.md
@D:/Projects/QA/tracy/electron/core/healing/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create crawler contracts and DOM skeleton hashing (CRAWL-02)</name>
  <files>electron/core/crawler/types.ts, electron/core/crawler/domSkeleton.ts, electron/core/crawler/domSkeleton.test.ts</files>
  <behavior>
    - Test 1: computeDomSkeletonHash returns consistent 64-bit/hex hash for identical structural DOMs regardless of volatile text/timestamps.
    - Test 2: computeDomSkeletonHash returns different hashes when layout or tags change (e.g., modal added or navigation menu expanded).
    - Test 3: extractStructuralSkeleton strips volatile attributes (style, inline scripts, dynamic ids) while retaining tag names, structural roles, and interactive hierarchy.
  </behavior>
  <action>
1. Create `electron/core/crawler/types.ts` defining:
   - `CrawlNode`: id (URL + skeleton hash), url, pathname, title, skeletonHash, visitedAt, interactiveElements, isTerminal.
   - `CrawlEdge`: sourceNodeId, targetNodeId, actionType ('click' | 'navigate' | 'fill_submit'), selector, triggerText.
   - `InteractiveElement`: tagName, type, role, selector, text, isSafe, href, formId, isSubmit.
   - `CrawlOptions`: maxDepth, maxPages, originBoundary (boolean/string), allowedPaths (RegExp[]), blockedPaths (RegExp[]), fillForms (boolean), timeoutMs.
   - `CrawlProgressEvent`: phase, currentNodeId, totalDiscovered, totalVisited, queueLength, currentUrl.
   - `DiscoveredFlow`: id, name, description, steps (YamlStep[]), startUrl, targetUrl.

2. Create `electron/core/crawler/domSkeleton.ts`:
   - Implement `extractStructuralSkeleton(domTreeOrHtml: string | Node)`: Strips text nodes, volatile attributes (id matching UUID/counters, random tokens, inline styles), preserving tags, structure, and semantic roles.
   - Implement `computeDomSkeletonHash(skeleton: string)`: Generates lightweight 32/64-bit cryptographic or FNV-1a/Murmur3/SHA256 hex hash for fast state comparison in Set/Map.

3. Write comprehensive unit tests in `electron/core/crawler/domSkeleton.test.ts`.
  </action>
  <verify>
    <automated>pnpm test electron/core/crawler/domSkeleton.test.ts</automated>
  </verify>
  <done>Contracts exported, skeleton hashing deterministic, tests passing green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement destructive action safety filter (CRAWL-03)</name>
  <files>electron/core/crawler/safetyFilter.ts, electron/core/crawler/safetyFilter.test.ts</files>
  <behavior>
    - Test 1: Blocks destructive words in text, aria-label, name, class, id, or href matching `/logout|delete|remove|destroy|signout|cancel|drop|terminate|reset|purge|unlink|unsubscribe/i`.
    - Test 2: Permits safe interactive elements (nav links, view details, search buttons, next page, filter dropdowns).
    - Test 3: filterSafeInteractiveElements returns only elements deemed safe with safety scores and reasons for blocked items.
  </behavior>
  <action>
1. Create `electron/core/crawler/safetyFilter.ts`:
   - Export `DANGEROUS_ACTION_PATTERN = /\b(logout|log-out|signout|sign-out|delete|remove|destroy|cancel|drop|terminate|reset|purge|unlink|unsubscribe|clear-all|trash)\b/i`.
   - Export `isDestructiveAction(element: Partial<InteractiveElement>): { isSafe: boolean; reason?: string }`:
     - Inspect `text`, `ariaLabel`, `name`, `id`, `className`, `href`, `title`, and `selector`.
     - Check if HTTP method in form is destructive (DELETE/PUT to dangerous endpoints).
     - Return `{ isSafe: false, reason: 'Matched dangerous term: ...' }` if flagged.
   - Export `filterSafeInteractiveElements(elements: InteractiveElement[]): { safe: InteractiveElement[]; blocked: Array<{ element: InteractiveElement; reason: string }> }`.

2. Write unit tests in `electron/core/crawler/safetyFilter.test.ts` checking positive and negative keyword matches across attributes.
  </action>
  <verify>
    <automated>pnpm test electron/core/crawler/safetyFilter.test.ts</automated>
  </verify>
  <done>Safety filter strictly rejects destructive keywords, allowing safe traversal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement safe synthetic form exploration (CRAWL-04)</name>
  <files>electron/core/crawler/formExplorer.ts, electron/core/crawler/formExplorer.test.ts</files>
  <behavior>
    - Test 1: Generates context-appropriate mock inputs based on input type/name/placeholder (email -> "test.user@example.com", search -> "test search", phone -> "555-0199", number -> "1", text -> "Sample Input").
    - Test 2: Correctly maps checkboxes, radios, select dropdowns with valid non-destructive options.
    - Test 3: planFormInteractions returns ordered fill and click steps targeting the form submit button if safe.
  </behavior>
  <action>
1. Create `electron/core/crawler/formExplorer.ts`:
   - Export `generateSyntheticFormData(field: { name?: string; type?: string; placeholder?: string; role?: string; options?: string[] }): string | boolean`.
   - Implement heuristics:
     - `email` or field name contains `email` -> `crawl.tester+demo@proqa.local`
     - `search` / `q` / `query` -> `sample search`
     - `phone` / `tel` -> `555-0100`
     - `zip` / `postal` -> `90210`
     - `url` -> `https://example.com`
     - `date` -> `2026-01-01`
     - `password` -> skip submission unless explicitly allowed or provide benign `TestPassword123!`
     - generic text -> `Test Field`
   - Export `planFormInteractions(formElement: { id?: string; selector: string; fields: any[]; submitButton?: InteractiveElement }): FlowStepPlan[]`.
   - Guard submission button with `isDestructiveAction()`.

2. Write unit tests in `electron/core/crawler/formExplorer.test.ts`.
  </action>
  <verify>
    <automated>pnpm test electron/core/crawler/formExplorer.test.ts</automated>
  </verify>
  <done>Synthetic form generator outputs contextual inputs and interaction plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted Web Page -> Crawler Engine | Target web page contains arbitrary DOM, forms, links, and onclick handlers. |
| Crawler -> Form Submissions | Crawler may send data to live endpoints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Elevation of Privilege / Tampering | `safetyFilter.ts` | mitigate | Block destructive keywords in all element attributes and prevent invoking logout/delete actions. |
| T-18-02 | Denial of Service | `domSkeleton.ts` | mitigate | Hash structural skeleton without memory leak; bound DOM tree depth and string size. |
| T-18-03 | Information Disclosure | `formExplorer.ts` | mitigate | Use only synthetic dummy data; never read or submit real credentials or secrets. |
</threat_model>

<verification>
Automated unit test runs:
- `pnpm test electron/core/crawler/`
- All tests pass with zero lint errors.
</verification>

<success_criteria>
Core crawler utilities created and tested: types, skeleton hashing, destructive action blocker, and synthetic form explorer.
</success_criteria>

<output>
Create `.planning/phases/18-autonomous-route-and-interaction-crawler/18-01-SUMMARY.md`
</output>
