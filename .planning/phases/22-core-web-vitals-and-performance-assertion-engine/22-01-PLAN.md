---
phase: 22-core-web-vitals-and-performance-assertion-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/flow.ts
  - docs/FLOW_SCHEMA.md
  - electron/core/perf/types.ts
  - electron/core/perf/injectedObserver.ts
  - electron/core/perf/throttlingManager.ts
  - electron/core/perf/perfObserverEngine.ts
  - electron/core/perf/perfObserverEngine.test.ts
autonomous: true
requirements:
  - PERF-01
  - PERF-02
  - PERF-03
  - PERF-04
must_haves:
  truths:
    - "In-browser script observer injected via page.addInitScript captures LCP, CLS, INP, FCP, and TTFB telemetry across all browsers (PERF-01)"
    - "Declarative performance assertion types and comparator evaluator support '<=', '<', '>=', '>', and unit parsing (ms, s, unitless score) for Core Web Vitals (PERF-02)"
    - "Observer gracefully degrades on WebKit and Firefox using W3C Navigation & Resource Timing without throwing CDP errors (PERF-03)"
    - "CDP throttling manager applies synthetic network presets (slow3g, fast3g, offline) and CPU slowdown rates on Chromium targets (PERF-04)"
  artifacts:
    - path: "src/types/flow.ts"
      provides: "Declarative assertPerformance schema and PerformanceCondition types in FlowStep"
      exports: ["PerformanceMetricKey", "PerformanceAssertionThreshold", "PerformanceBudget", "AssertPerformanceStep", "ThrottlingPreset"]
    - path: "electron/core/perf/types.ts"
      provides: "Performance telemetry data models, throttling configuration, and assertion evaluation interfaces"
      exports: ["WebVitalsMetrics", "PerformanceAssertionResult", "ThrottlingConfig", "PerfEvaluationReport"]
    - path: "electron/core/perf/injectedObserver.ts"
      provides: "Self-contained script string injecting PerformanceObserver and W3C Navigation/Resource Timing telemetry fallback"
      exports: ["PERF_OBSERVER_INIT_SCRIPT", "extractNavigationTimingMetrics"]
    - path: "electron/core/perf/throttlingManager.ts"
      provides: "CDP network and CPU emulation controller with browser engine safeguards"
      exports: ["ThrottlingManager", "THROTTLING_PRESETS"]
    - path: "electron/core/perf/perfObserverEngine.ts"
      provides: "PerfObserverEngine orchestrating telemetry injection, metric harvesting, comparator evaluation, and throttling"
      exports: ["PerfObserverEngine", "evaluatePerformanceAssertion", "parseThresholdExpression"]
    - path: "electron/core/perf/perfObserverEngine.test.ts"
      provides: "Unit test suite verifying metric extraction, fallback handling, threshold comparator parsing, and CDP throttling guards"
    - path: "docs/FLOW_SCHEMA.md"
      provides: "Documentation for assertPerformance action, metric targets, and throttling frontmatter/step syntax"
  key_links:
    - from: "electron/core/perf/perfObserverEngine.ts"
      to: "electron/core/perf/injectedObserver.ts"
      via: "Injected script initialization"
      pattern: "PERF_OBSERVER_INIT_SCRIPT"
    - from: "electron/core/perf/perfObserverEngine.ts"
      to: "electron/core/perf/throttlingManager.ts"
      via: "ThrottlingManager delegation"
      pattern: "new ThrottlingManager"
    - from: "src/types/flow.ts"
      to: "docs/FLOW_SCHEMA.md"
      via: "YAML schema alignment for assertPerformance"
---

<objective>
Build the core performance telemetry and assertion engine (`PerfObserverEngine`), in-browser `PerformanceObserver` script with W3C Navigation Timing fallback (`injectedObserver.ts`), CDP network/CPU throttling manager (`throttlingManager.ts`), and threshold comparator evaluator (`evaluatePerformanceAssertion`), updating `src/types/flow.ts` and `docs/FLOW_SCHEMA.md`.

Purpose: Provide robust Core Web Vitals extraction (LCP, CLS, INP, FCP, TTFB), budget threshold assertion evaluation, and network/CPU throttling for deterministic performance regression testing across Chromium, Firefox, and WebKit.
Output: Flow type definitions, `PerfObserverEngine` core module, throttling manager, threshold evaluation utility, schema docs, and comprehensive Vitest test suite.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/research/SUMMARY.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@src/types/flow.ts
@docs/FLOW_SCHEMA.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Performance Types, Schema Contracts, and Threshold Expression Parser (PERF-02, PERF-04)</name>
  <files>src/types/flow.ts, docs/FLOW_SCHEMA.md, electron/core/perf/types.ts</files>
  <behavior>
    - Test 1: FlowStep accepts 'assertPerformance' and 'throttle' CommandTypes.
    - Test 2: AssertPerformanceStep defines budget rules for lcp, cls, inp, fcp, ttfb, domContentLoaded, loadTime, jsHeapSize.
    - Test 3: FlowMetadata accepts optional `throttling?: ThrottlingPreset | ThrottlingConfig`.
    - Test 4: electron/core/perf/types.ts exports WebVitalsMetrics, PerformanceAssertionResult, ThrottlingConfig, and PerfEvaluationReport.
  </behavior>
  <action>
1. Update `src/types/flow.ts`:
   - Extend `CommandType` union with `'assertPerformance' | 'throttle'`.
   - Export `PerformanceMetricKey = 'lcp' | 'cls' | 'inp' | 'fcp' | 'ttfb' | 'domContentLoaded' | 'loadTime' | 'domNodes' | 'jsHeapSize'`.
   - Export `PerformanceAssertionThreshold = number | string` (e.g. `2500`, `"< 2500ms"`, `"<= 0.1"`, `"> 100"`).
   - Export `PerformanceBudget = Partial<Record<PerformanceMetricKey, PerformanceAssertionThreshold>>`.
   - Export `ThrottlingPreset = 'slow3g' | 'fast3g' | 'offline' | 'none'`.
   - Export `ThrottlingConfig`:
     ```typescript
     export interface ThrottlingConfig {
       preset?: ThrottlingPreset;
       latencyMs?: number;
       downloadKbps?: number;
       uploadKbps?: number;
       cpuSlowdownRate?: number; // e.g. 4 for 4x slowdown
       offline?: boolean;
     }
     ```
   - Export `AssertPerformanceStep`:
     ```typescript
     export interface AssertPerformanceStep extends PerformanceBudget {
       action?: 'assertPerformance';
       warnOnly?: boolean;
       message?: string;
     }
     ```
   - Update `FlowMetadata`:
     - Add `throttling?: ThrottlingPreset | ThrottlingConfig;`
     - Add `performanceBudget?: PerformanceBudget;`
2. Create `electron/core/perf/types.ts`:
   - Export `WebVitalsMetrics`:
     ```typescript
     export interface WebVitalsMetrics {
       lcp?: number;
       cls?: number;
       inp?: number;
       fcp?: number;
       ttfb?: number;
       domContentLoaded?: number;
       loadTime?: number;
       domNodes?: number;
       jsHeapSize?: number;
       navigationTiming?: {
         dnsMs?: number;
         tcpMs?: number;
         requestMs?: number;
         responseMs?: number;
         domProcessingMs?: number;
         loadEventMs?: number;
       };
       timestamp: number;
       syntheticFallback: boolean;
       browserEngine: 'chromium' | 'firefox' | 'webkit';
     }
     ```
   - Export `ParsedThreshold`:
     ```typescript
     export interface ParsedThreshold {
       operator: '<' | '<=' | '>' | '>=' | '==' | '=';
       value: number;
       unit?: string;
     }
     ```
   - Export `PerformanceAssertionResult`:
     ```typescript
     export interface MetricAssertionDetail {
       metric: PerformanceMetricKey;
       actual: number;
       threshold: number;
       operator: string;
       passed: boolean;
       unit: string;
       rating: 'good' | 'needs-improvement' | 'poor';
       message?: string;
     }

     export interface PerformanceAssertionResult {
       passed: boolean;
       warnOnly?: boolean;
       metrics: WebVitalsMetrics;
       assertions: MetricAssertionDetail[];
       failedAssertions: MetricAssertionDetail[];
       summary: string;
     }
     ```
3. Update `docs/FLOW_SCHEMA.md`:
   - Document `assertPerformance` action with examples (`lcp: "< 2500ms"`, `cls: "<= 0.1"`, `ttfb: "< 800ms"`).
   - Document `throttling:` frontmatter key and inline `throttle` action (`preset: slow3g`, `cpuSlowdownRate: 4`).
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>Performance schema types, telemetry data models, and documentation contracts defined.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build Injected PerformanceObserver Script, CDP Throttling Manager, and PerfObserverEngine (PERF-01, PERF-02, PERF-03, PERF-04)</name>
  <files>electron/core/perf/injectedObserver.ts, electron/core/perf/throttlingManager.ts, electron/core/perf/perfObserverEngine.ts, electron/core/perf/perfObserverEngine.test.ts</files>
  <behavior>
    - Test 1: parseThresholdExpression correctly parses number ("< 2500ms" -> { operator: '<', value: 2500, unit: 'ms' }, "<= 0.1" -> { operator: '<=', value: 0.1 }, 120 -> { operator: '<=', value: 120 }).
    - Test 2: evaluatePerformanceAssertion evaluates metrics against budget rules and classifies ratings (good/needs-improvement/poor).
    - Test 3: injectedObserver script captures PerformanceObserver entries and falls back to performance.timing / performance.getEntriesByType('navigation') when LCP/CLS/INP observer is not supported.
    - Test 4: ThrottlingManager translates presets ('slow3g', 'fast3g', 'offline') to CDP Network and Emulation parameters on Chromium, and safely skips on Firefox/WebKit without throwing errors.
    - Test 5: PerfObserverEngine attaches to BrowserContext/Page, collects WebVitalsMetrics on demand or at end of navigation, and cleans up handlers.
  </behavior>
  <action>
1. Create `electron/core/perf/injectedObserver.ts`:
   - Export `PERF_OBSERVER_INIT_SCRIPT` string containing IIFE:
     - Check `if (window.__tracyPerfAttached) return; window.__tracyPerfAttached = true;`
     - Maintain in-window state `window.__tracyMetrics = { lcp: 0, cls: 0, inp: 0, fcp: 0, ttfb: 0, domContentLoaded: 0, loadTime: 0, domNodes: 0 }`.
     - Layout Shift observer (`layout-shift`): accumulates non-input shift scores into `cls`.
     - LCP observer (`largest-contentful-paint`): tracks `lastEntry.renderTime || lastEntry.loadTime`.
     - Paint observer (`paint`): extracts `first-contentful-paint`.
     - Event Timing observer (`event` / `first-input`): tracks `duration` for INP / FID.
     - Fallback / Navigation Timing: calculate `ttfb` (`responseStart - requestStart` or `responseStart - fetchStart`), `domContentLoaded`, and `loadTime` from `performance.getEntriesByType('navigation')[0]` or `window.performance.timing`.
     - Count DOM elements: `document.getElementsByTagName('*').length`.
     - Expose helper `window.__tracyGetMetrics()` returning normalized metrics snapshot.
2. Create `electron/core/perf/throttlingManager.ts`:
   - Define preset configurations in `THROTTLING_PRESETS`:
     - `slow3g`: `latency: 400`, `downloadThroughput: 400 * 1024 / 8` (400 kbps), `uploadThroughput: 400 * 1024 / 8`, `connectionType: 'cellular3g'`.
     - `fast3g`: `latency: 150`, `downloadThroughput: 1.6 * 1024 * 1024 / 8` (1.6 Mbps), `uploadThroughput: 750 * 1024 / 8` (750 kbps), `connectionType: 'cellular3g'`.
     - `offline`: `offline: true`, `latency: 0`, `downloadThroughput: 0`, `uploadThroughput: 0`.
   - Implement `ThrottlingManager`:
     - `async applyThrottling(page: Page, config: ThrottlingConfig | ThrottlingPreset, browserEngine: string): Promise<boolean>`:
       - If `browserEngine !== 'chromium'`, log warning/debug and return `false` (never throw CDP divergence error per Pitfall 1).
       - Create `session = await page.context().newCDPSession(page)`.
       - Resolve config from preset name or custom object.
       - Send `Network.emulateNetworkConditions` with latency/bandwidth parameters.
       - If `cpuSlowdownRate > 1`, send `Emulation.setCPUThrottlingRate`.
       - Return `true`.
     - `async clearThrottling(page: Page, browserEngine: string): Promise<void>`:
       - If `browserEngine === 'chromium'`, reset network emulation and CPU throttling rate to 1.
3. Create `electron/core/perf/perfObserverEngine.ts`:
   - Implement `parseThresholdExpression(expr: PerformanceAssertionThreshold): ParsedThreshold`:
     - Handles strings like `"< 2500ms"`, `"<= 2.5s"`, `"> 0"`, `">= 100"`, `"0.1"`, or plain numbers.
     - Normalizes unit scaling (`s` -> `* 1000` for time metrics).
   - Implement `classifyMetricRating(metric: PerformanceMetricKey, value: number): 'good' | 'needs-improvement' | 'poor'`:
     - LCP: <= 2500 good, <= 4000 needs-improvement, > 4000 poor.
     - CLS: <= 0.1 good, <= 0.25 needs-improvement, > 0.25 poor.
     - INP: <= 200 good, <= 500 needs-improvement, > 500 poor.
     - FCP: <= 1800 good, <= 3000 needs-improvement, > 3000 poor.
     - TTFB: <= 800 good, <= 1800 needs-improvement, > 1800 poor.
   - Implement `evaluatePerformanceAssertion(metrics: WebVitalsMetrics, budget: PerformanceBudget, warnOnly?: boolean): PerformanceAssertionResult`.
   - Implement `PerfObserverEngine` class:
     - `async attachToContext(context: BrowserContext, browserEngine: 'chromium' | 'firefox' | 'webkit'): Promise<void>`:
       - Injects `PERF_OBSERVER_INIT_SCRIPT` via `context.addInitScript(...)`.
     - `async harvestMetrics(page: Page, browserEngine: 'chromium' | 'firefox' | 'webkit'): Promise<WebVitalsMetrics>`:
       - Force visibility flush if Chromium/Page to settle metrics: `await page.evaluate(() => { try { Object.defineProperty(document, 'visibilityState', { value: 'hidden', writable: true }); document.dispatchEvent(new Event('visibilitychange')); } catch(e){} })`.
       - Execute `window.__tracyGetMetrics()` inside page.
       - If Chromium, query CDP session for `Performance.getMetrics` to get `JSHeapUsedSize` and exact node counts if available.
       - Construct and return `WebVitalsMetrics` object.
     - `async applyThrottling(page: Page, config: ThrottlingConfig | ThrottlingPreset, browserEngine: string)`: delegates to `ThrottlingManager`.
4. Write unit tests in `electron/core/perf/perfObserverEngine.test.ts`:
   - Test threshold expression parsing with all comparison operators and units (`ms`, `s`, decimals).
   - Test assertion evaluation with passing, failing, and warning budget rules.
   - Test rating classifications for standard Core Web Vitals thresholds.
   - Test throttling manager CDP invocation on Chromium and safe bypass on Firefox/WebKit.
   - Test script injection and fallback metric extraction.
  </action>
  <verify>
    <automated>pnpm test electron/core/perf/perfObserverEngine.test.ts</automated>
  </verify>
  <done>Injected observer script, CDP throttling manager, and PerfObserverEngine implemented and thoroughly tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Injected Script -> Host Page DOM | Script runs in page context to read PerformanceObserver entries. |
| Host Playwright Runner -> CDP Session | Direct Chrome DevTools Protocol commands for CPU/Network throttling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Denial of Service (CDP Crash) | `throttlingManager.ts` | mitigate | Strict engine guards (`browserEngine === 'chromium'`) preventing CDP calls on Firefox/WebKit. |
| T-22-02 | Tampering (Page Scope Pollution) | `injectedObserver.ts` | mitigate | Encapsulate observer logic in an isolated IIFE and namespace all exposed window properties under `__tracyMetrics`. |
| T-22-03 | Denial of Service (Infinite Throttle) | `throttlingManager.ts` | mitigate | Enforce automatic throttle cleanup and reset on test completion in finally blocks. |
</threat_model>

<verification>
- `pnpm test electron/core/perf/perfObserverEngine.test.ts` passes
- `pnpm typecheck` passes with zero errors
</verification>

<success_criteria>
In-browser Core Web Vitals telemetry collector and performance assertion evaluator are fully implemented with cross-browser fallback and CDP throttling capabilities.
</success_criteria>

<output>
After completion, create `.planning/phases/22-core-web-vitals-and-performance-assertion-engine/22-01-PLAN.md`
</output>
