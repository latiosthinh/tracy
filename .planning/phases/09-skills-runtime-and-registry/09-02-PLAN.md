---
phase: 09-skills-runtime-and-registry
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - electron/preload.ts
  - electron/ipc/fileSystem.ts
  - src/lib/ipc.ts
  - src/lib/skills/builtins/index.ts
  - src/lib/skills/registry.ts
  - src/lib/skills/registry.test.ts
  - src/stores/agentStore.ts
  - src/stores/agentStore.test.ts
autonomous: true
requirements:
  - SKILL-02

must_haves:
  truths:
    - "Registry supports built-in bundled skills and dynamic project-level custom skills discovered via IPC"
    - "Active skill set is accessible to AI providers and Copilot UI via Zustand store (agentStore)"
    - "Ability to toggle skills individually or select preset skill packs (Standard QA, Form Specialist, Data Table Deep-Dive, Full Power)"
  artifacts:
    - path: "src/lib/skills/registry.ts"
      provides: "In-memory skills registry management and prompt/tool compilation helpers"
      exports: ["SkillRegistry", "skillRegistry", "compileSkillsPrompt", "compileSkillsTools"]
    - path: "src/lib/skills/builtins/index.ts"
      provides: "Starter built-in skill definitions scaffolding for standard domains"
      exports: ["BUILTIN_SKILLS", "SKILL_PRESETS"]
    - path: "src/stores/agentStore.ts"
      provides: "State management for active skills, project skills, and preset selection"
      contains: "activeSkillIds"
  key_links:
    - from: "src/stores/agentStore.ts"
      to: "src/lib/skills/registry.ts"
      via: "skillRegistry.getAll() and compile helpers"
      pattern: "skillRegistry"
    - from: "src/lib/ipc.ts"
      to: "electron/preload.ts"
      via: "IPC channel load_project_skills"
      pattern: "load_project_skills"
---

<objective>
Build the Skills Registry engine with built-in skill definitions scaffolding, extend Electron main IPC to discover project custom skills from `.proqa/skills/`, and update `agentStore` to manage active skills and preset selection.

Purpose: Deliver the centralized skill management system (SKILL-02) that powers skill discovery, enabling/disabling, preset packs, and dynamic prompt injection across Tracy's AI layer.
Output: `src/lib/skills/builtins/index.ts`, `src/lib/skills/registry.ts`, updated IPC bridge (`electron/ipc/fileSystem.ts`, `electron/preload.ts`, `src/lib/ipc.ts`), updated `src/stores/agentStore.ts`, and full test coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/09-skills-runtime-and-registry/09-CONTEXT.md
@.planning/phases/09-skills-runtime-and-registry/09-01-PLAN.md
@src/types/skills.ts
@src/lib/skills/schema.ts
@src/lib/skills/serializer.ts
@src/stores/agentStore.ts
@src/lib/ipc.ts
@electron/preload.ts
@electron/ipc/fileSystem.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Built-in Skills Catalog Scaffolding & SkillRegistry Engine</name>
  <files>src/lib/skills/builtins/index.ts, src/lib/skills/registry.ts, src/lib/skills/registry.test.ts</files>
  <behavior>
    - Test SkillRegistry registers built-in skills on instantiation
    - Test SkillRegistry registers and unregisters custom skills dynamically
    - Test SkillRegistry queries skills by id, domain, or tag
    - Test compileSkillsPrompt combines systemPromptInjection from all active skill IDs cleanly
    - Test compileSkillsTools aggregates tool definitions from active skills without duplication
    - Test SKILL_PRESETS definition contains 'standard-qa', 'form-specialist', 'table-deepdive', and 'full-power'
  </behavior>
  <action>
    1. Create `src/lib/skills/builtins/index.ts` defining starter built-in skill definitions (`auth-resilience`, `form-validation`, `table-pagination`, `shadow-dom-modal`, `generic-qa`) and standard preset definitions (`SKILL_PRESETS`).
    2. Create `src/lib/skills/registry.ts` with `SkillRegistry` class:
       - Internal map of registered skills.
       - Methods: `registerSkill`, `unregisterSkill`, `getSkill`, `getAllSkills`, `getSkillsByDomain`, `compilePrompt(activeIds)`, `compileTools(activeIds)`.
       - Export singleton instance `skillRegistry`.
    3. Write comprehensive unit tests in `src/lib/skills/registry.test.ts`.
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/skills/registry.test.ts</automated>
  </verify>
  <done>SkillRegistry and built-in scaffolding tested and working with all registration and prompt compilation tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Project Custom Skills IPC & agentStore Integration</name>
  <files>electron/preload.ts, electron/ipc/fileSystem.ts, src/lib/ipc.ts, src/stores/agentStore.ts, src/stores/agentStore.test.ts</files>
  <behavior>
    - Test load_project_skills IPC scans `.proqa/skills/` within project saveLocation for `.skill.json` and `.skill.yaml` files safely
    - Test invalid or corrupted skill files in workspace directory are returned with parse error warnings rather than crashing
    - Test agentStore holds activeSkillIds, customSkills, and active preset pack state
    - Test agentStore toggleSkill, setPreset, and loadProjectSkills actions update state properly
    - Test agentStore computes compiled system prompt and tool definitions from active skills
  </behavior>
  <action>
    1. In `electron/ipc/fileSystem.ts`, add handler for `load_project_skills` that scans `<saveLocation>/.proqa/skills` directory, reads `.skill.json` and `.skill.yaml` files, parses with safe schema validation, and returns list of custom `SkillDefinition` objects + any warnings.
    2. In `electron/preload.ts`, add `load_project_skills` to `ALLOWED_INVOKE_CHANNELS`.
    3. In `src/lib/ipc.ts`, add `loadProjectSkills(saveLocation: string): Promise<{ skills: SkillDefinition[]; warnings: string[] }>` to `tracyApi` with browser fallback stub.
    4. In `src/stores/agentStore.ts`, extend `AgentState`:
       - `activeSkillIds: string[]` (defaulting to Standard QA preset)
       - `customSkills: SkillDefinition[]`
       - `activePreset: string | null`
       - `toggleSkill: (skillId: string) => void`
       - `setActiveSkills: (skillIds: string[]) => void`
       - `setPreset: (presetId: string) => void`
       - `loadCustomSkills: (saveLocation: string) => Promise<void>`
       - `getCompiledPrompt: () => string`
       - `getCompiledTools: () => ToolDefinition[]`
    5. Write unit tests in `src/stores/agentStore.test.ts`.
  </action>
  <verify>
    <automated>pnpm vitest run src/stores/agentStore.test.ts</automated>
  </verify>
  <done>IPC channel registered, agentStore updated with full skill management, and unit tests passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer <-> Main IPC (`load_project_skills`) | Renderer requests main process to read files in workspace directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Path Traversal | `electron/ipc/fileSystem.ts` (`load_project_skills`) | mitigate | Enforce `resolveSafeBase` and `assertSafePath` to ensure directory traversal cannot escape workspace root. |
| T-09-04 | Denial of Service / Crash | `electron/ipc/fileSystem.ts` | mitigate | Catch file read/parse errors per file, return diagnostic warnings array rather than failing entire IPC call. |
</threat_model>

<verification>
Run full lint, typecheck, and test suite:
`pnpm lint && pnpm test`
</verification>

<success_criteria>
1. `pnpm typecheck` and `pnpm lint` pass with 0 errors/warnings.
2. `load_project_skills` IPC whitelisted and functional.
3. `agentStore` manages active skills, presets, and dynamic compilation.
4. Fulfills SKILL-02 requirement.
</success_criteria>

<output>
After completion, create `.planning/phases/09-skills-runtime-and-registry/09-02-SUMMARY.md`
</output>
