---
phase: 02-a11y-and-text-extraction
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - src/a11y/en.json
  - src/components/ai/AiCopilot.tsx
  - src/components/ai/AiPromptInput.tsx
  - src/components/ai/VoiceInputButton.tsx
  - src/components/editor/YamlEditor.tsx
  - src/components/editor/VisualStepEditor.tsx
  - src/components/editor/FlowCategorySelector.tsx
  - src/components/reports/TestReports.tsx
  - src/components/reports/CliTerminal.tsx
  - src/components/shared/SplashScreen.tsx
  - src/components/shared/LoadingSpinner.tsx
  - src/a11y/a11yTextGuard.test.ts
autonomous: true
requirements:
  - A11Y-01
  - A11Y-02
  - A11Y-03
  - A11Y-04
---

<objective>
Complete text extraction across remaining AI Copilot, YAML/Visual Editors, and Reports components, then implement the automated text guard test to enforce zero hardcoded text regressions forever.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Extract strings and add a11y across AI, Editor, Reports, and Splash</name>
  <files>src/a11y/en.json, src/components/ai/AiCopilot.tsx, src/components/ai/AiPromptInput.tsx, src/components/ai/VoiceInputButton.tsx, src/components/editor/YamlEditor.tsx, src/components/editor/VisualStepEditor.tsx, src/components/editor/FlowCategorySelector.tsx, src/components/reports/TestReports.tsx, src/components/reports/CliTerminal.tsx, src/components/shared/SplashScreen.tsx, src/components/shared/LoadingSpinner.tsx</files>
  <action>
    1. Populate `copilot`, `editor`, `reports`, and `splash` sections in `src/a11y/en.json`.
    2. Replace all buttons, labels, placeholders, titles, and step descriptions with `t()`.
    3. Add `aria-live="polite"` on streaming output and execution terminal logs.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement no-hardcoded-text guard test</name>
  <files>src/a11y/a11yTextGuard.test.ts</files>
  <action>
    1. Write `src/a11y/a11yTextGuard.test.ts` scanning all `.tsx` files in `src/components/`.
    2. Test checks for raw text nodes in JSX outside allowable exceptions (e.g. icon glyphs, dynamic props, code blocks).
    3. Verify all 40 component files pass cleanly.
  </action>
  <verify>
    <automated>pnpm test src/a11y/a11yTextGuard.test.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Full quality gate audit</name>
  <files></files>
  <action>
    1. Run `pnpm lint` to ensure zero ESLint and `jsx-a11y` errors.
    2. Run `pnpm test` to ensure 100% of unit tests pass including the new a11y guard test.
    3. Run `pnpm exec vite build` to ensure all production bundles build cleanly.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }; if ($?) { pnpm exec vite build }</automated>
  </verify>
</task>

</tasks>

<output>
Create `.planning/phases/02-a11y-and-text-extraction/02-04-SUMMARY.md` on completion.
</output>
