---
phase: 13-copilot-skill-and-trace-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/utils/traceSanitizer.ts
  - src/utils/traceSanitizer.test.ts
  - src/components/ai/TraceInspector.tsx
  - src/components/ai/TraceInspector.test.tsx
  - src/components/ai/AiCopilot.tsx
  - src/components/ai/AiCopilot.test.tsx
  - src/a11y/en.json
autonomous: true
requirements:
  - UI-02

must_haves:
  truths:
    - "User can observe real-time agent reasoning steps, tool calls, and selector checks during generation"
    - "Tool arguments and results with sensitive values (passwords, tokens, API keys) are masked before rendering"
    - "Trace Inspector collapses and expands smoothly with clear status badges and turn counters"
    - "All trace events stream dynamically over tracyApi.onAgentToolTrace IPC subscription"
  artifacts:
    - path: "src/utils/traceSanitizer.ts"
      provides: "Recursive credential and secret redaction for agent tool traces and thought logs"
      min_lines: 30
    - path: "src/utils/traceSanitizer.test.ts"
      provides: "Unit tests verifying thorough redaction of passwords, tokens, API keys, and authorization headers"
      min_lines: 40
    - path: "src/components/ai/TraceInspector.tsx"
      provides: "Collapsible real-time trace inspector component with live status badges and execution metrics"
      min_lines: 80
    - path: "src/components/ai/TraceInspector.test.tsx"
      provides: "Unit test suite for TraceInspector rendering, tool event display, redaction, and collapse toggle"
      min_lines: 50
  key_links:
    - from: "src/components/ai/TraceInspector.tsx"
      to: "src/utils/traceSanitizer.ts"
      via: "redactSensitiveData"
      pattern: "redactSensitiveData"
    - from: "src/components/ai/AiCopilot.tsx"
      to: "src/lib/ipc.ts"
      via: "tracyApi.onAgentToolTrace"
      pattern: "onAgentToolTrace"
    - from: "src/components/ai/AiCopilot.tsx"
      to: "src/components/ai/TraceInspector.tsx"
      via: "JSX component embedding"
      pattern: "<TraceInspector"
---

<objective>
Implement `traceSanitizer.ts`, `TraceInspector.tsx` component, real-time IPC trace event listener in `AiCopilot.tsx`, a11y translations in `src/a11y/en.json`, and comprehensive unit tests.

Purpose: Provide QA engineers with full visibility into the AI agent's self-healing thought process, DOM validation tool calls, and locator verification results with zero risk of credential leakage.
Output: `src/utils/traceSanitizer.ts`, `src/utils/traceSanitizer.test.ts`, `src/components/ai/TraceInspector.tsx`, `src/components/ai/TraceInspector.test.tsx`, updated `AiCopilot.tsx`, updated `en.json`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-copilot-skill-and-trace-ui/13-CONTEXT.md
@src/types/skills.ts
@src/lib/ipc.ts
@src/a11y/en.json
@src/components/ai/AiCopilot.tsx

<interfaces>
From src/types/skills.ts:
```typescript
export interface AgentToolCall {
  id?: string;
  name: string;
  arguments: Record<string, unknown>;
}

export interface AgentToolResult {
  valid: boolean;
  matchCount: number;
  details?: unknown;
  error?: string;
}

export interface AgentToolTraceEvent {
  turn: number;
  thought?: string;
  toolCall?: AgentToolCall;
  toolResult?: AgentToolResult;
  timestamp: string;
}
```

From src/lib/ipc.ts:
```typescript
onAgentToolTrace: async (callback: (payload: AgentToolTraceEvent) => void): Promise<UnlistenFn>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement traceSanitizer utility and unit test suite</name>
  <files>src/utils/traceSanitizer.ts, src/utils/traceSanitizer.test.ts</files>
  <behavior>
    - Test 1: Redacts keys matching sensitive patterns (password, passwd, token, secret, apiKey, key, authorization, bearer, credential) in plain objects.
    - Test 2: Recursively redacts nested objects and arrays within tool arguments and results.
    - Test 3: Redacts inline secrets and authorization headers matching bearer token patterns inside text/thought strings.
    - Test 4: Preserves non-sensitive selectors, element tags, and test IDs without altering valid probe data.
  </behavior>
  <action>
1. Create `src/utils/traceSanitizer.ts`:
   - Export `redactSensitiveData(value: unknown): unknown`.
   - Implement recursive traversal of objects and arrays.
   - For string values under sensitive key names (`password`, `pass`, `token`, `secret`, `apiKey`, `key`, `auth`, `authorization`, `credential`), replace value with `'[REDACTED]'`.
   - In thought strings or unstructured text, replace patterns like `Bearer [a-zA-Z0-9._-]+` and `password[:=]\s*["']?[^"'\s]+` with masked equivalents.
   - Export `sanitizeTraceEvent(event: AgentToolTraceEvent): AgentToolTraceEvent` returning a sanitized clone.
2. Create `src/utils/traceSanitizer.test.ts` testing all redaction edge cases.
  </action>
  <verify>
    <automated>pnpm test src/utils/traceSanitizer.test.ts</automated>
  </verify>
  <done>traceSanitizer reliably masks credentials across objects and strings without mutating original data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create TraceInspector component, a11y translations, and unit tests</name>
  <files>src/a11y/en.json, src/components/ai/TraceInspector.tsx, src/components/ai/TraceInspector.test.tsx</files>
  <behavior>
    - Test 1: Renders empty state when no trace events are present.
    - Test 2: Renders list of sanitized trace events with turn numbers, thought bubbles, and tool badges.
    - Test 3: Displays tool call name (`validate_selector`, `find_elements_by_text`, etc.), formatted arguments, result match count, and success/error status.
    - Test 4: Accordion expand/collapse toggle switches expanded state and updates aria-expanded.
    - Test 5: Displays live pulsing indicator when `isGenerating` is true.
  </behavior>
  <action>
1. Update `src/a11y/en.json` under `"copilot"` with `"trace"` translations:
   - `title`: "Agent Reasoning & Tool Trace"
   - `turnLabel`: "Turn {turn}"
   - `thoughtLabel`: "Reasoning Thought"
   - `toolCallLabel`: "Tool: {name}"
   - `argumentsLabel`: "Arguments"
   - `resultLabel`: "Result ({count} matches)"
   - `noTraces`: "No reasoning traces recorded for this generation run."
   - `liveRunning`: "Agent is reasoning and validating selectors..."
   - `clearTraces`: "Clear traces"
   - `toggleCollapseAria`: "Toggle reasoning trace inspector"
   - `matchSuccess`: "Valid ({count} matches)"
   - `matchFailed`: "No elements matched"
2. Create `src/components/ai/TraceInspector.tsx`:
   - Props: `events: AgentToolTraceEvent[]`, `isGenerating: boolean`, `onClear?: () => void`.
   - Collapsible panel with header showing event count, active live badge when `isGenerating`, and expand/collapse chevron.
   - For each trace event, render turn chip, sanitized thought section (in styled thought bubble / blockquote), tool call badge with icon (Terminal/Wrench for tool, CheckCircle2 / AlertTriangle for toolResult status), pretty-printed JSON arguments (collapsible or formatted cleanly), and match result summary.
   - ARIA live region (`aria-live="polite"`) for real-time trace additions.
3. Create `src/components/ai/TraceInspector.test.tsx` testing render states, expand/collapse, tool payload display, and sanitized outputs.
  </action>
  <verify>
    <automated>pnpm test src/components/ai/TraceInspector.test.tsx</automated>
  </verify>
  <done>TraceInspector displays real-time agent reasoning steps, sanitizes credentials, and passes all unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Connect TraceInspector to real-time IPC in AiCopilot and update integration tests</name>
  <files>src/components/ai/AiCopilot.tsx, src/components/ai/AiCopilot.test.tsx</files>
  <action>
1. In `src/components/ai/AiCopilot.tsx`:
   - Add local state `traceEvents: AgentToolTraceEvent[]`.
   - In `useEffect`, subscribe to `tracyApi.onAgentToolTrace((event) => setTraceEvents(prev => [...prev, sanitizeTraceEvent(event)]))` during generation or mounted lifecycle.
   - Clear `traceEvents` when user starts new generation in `handleGenerateFlow` and `handleAutoSuite`.
   - Render `<TraceInspector events={traceEvents} isGenerating={isGenerating} onClear={() => setTraceEvents([])} />` directly beneath the prompt submission form / above streaming YAML result.
2. In `src/components/ai/AiCopilot.test.tsx`:
   - Add integration test for trace stream receiving `onAgentToolTrace` events and rendering them in `TraceInspector`.
   - Verify all tests pass cleanly.
  </action>
  <verify>
    <automated>pnpm test src/components/ai/AiCopilot.test.tsx</automated>
  </verify>
  <done>AiCopilot streams live trace events into TraceInspector via IPC, auto-clears on new runs, and passes all tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Electron Main IPC -> Renderer UI | Tool traces containing raw arguments (selectors, inputs) stream to renderer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Information Disclosure | TraceInspector toolCall arguments | mitigate | Run `sanitizeTraceEvent` / `redactSensitiveData` before saving or rendering trace payloads in UI |
| T-13-04 | Denial of Service | Trace event flooding in renderer | mitigate | Bound trace list size and render formatted JSON with max height scrolling containers |
</threat_model>

<verification>
`pnpm test src/utils/traceSanitizer.test.ts src/components/ai/TraceInspector.test.tsx src/components/ai/AiCopilot.test.tsx`
`pnpm typecheck`
`pnpm lint`
</verification>

<success_criteria>
- `traceSanitizer.ts` strips passwords, bearer tokens, and secrets from trace events.
- `TraceInspector.tsx` renders collapsible agent thoughts and tool calls with live updates.
- `AiCopilot.tsx` listens to `tracyApi.onAgentToolTrace` and passes sanitized events to `TraceInspector`.
- All tests, typechecks, and linter checks pass without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/13-copilot-skill-and-trace-ui/13-02-SUMMARY.md`
</output>
