---
phase: 06-editor-polish-and-exporter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/diffUtils.ts
  - src/utils/diffUtils.test.ts
  - src/components/editor/YamlDiffModal.tsx
  - src/components/editor/YamlEditor.tsx
  - src/a11y/en.json
autonomous: true
requirements:
  - EDIT-01

must_haves:
  truths:
    - "diffUtils accurately computes line additions, deletions, and modifications between two YAML text strings"
    - "YamlDiffModal displays a side-by-side or unified visual diff with color-coded additions (green) and deletions (red)"
    - "Users can revert changes back to original or copy modified text"
    - "All text strings flow through en.json via useTranslation"
---

<objective>
Build lightweight line diff engine (`src/utils/diffUtils.ts`), comprehensive unit tests, and the `YamlDiffModal.tsx` component to compare active YAML against saved baseline.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build diffUtils line diff engine with unit tests</name>
  <files>src/utils/diffUtils.ts, src/utils/diffUtils.test.ts</files>
  <action>
    1. In `src/utils/diffUtils.ts`, implement `computeLineDiff(originalText: string, modifiedText: string): DiffLine[]` with line types (`'added' | 'removed' | 'unchanged' | 'modified'`).
    2. Write unit tests in `src/utils/diffUtils.test.ts` testing additions, deletions, replacements, and identical inputs.
  </action>
  <verify>
    <automated>pnpm test src/utils/diffUtils.test.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Build YamlDiffModal and trigger in YamlEditor</name>
  <files>src/components/editor/YamlDiffModal.tsx, src/components/editor/YamlEditor.tsx, src/a11y/en.json</files>
  <action>
    1. Create `src/components/editor/YamlDiffModal.tsx` using `Modal.tsx` with side-by-side comparison panels, line numbers, and diff summary badges.
    2. In `src/a11y/en.json`, add translation keys for `diff.title`, `diff.original`, `diff.modified`, `diff.revert`, `diff.noChanges`, `diff.additions`, `diff.deletions`.
    3. In `src/components/editor/YamlEditor.tsx`, add a "View Diff" button (`GitCompare` icon) in the editor header when modified content differs from initial flow content.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }</automated>
  </verify>
</task>

</tasks>

<output>
Create `.planning/phases/06-editor-polish-and-exporter/06-01-SUMMARY.md` on completion.
</output>
