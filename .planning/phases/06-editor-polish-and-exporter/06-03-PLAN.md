---
phase: 06-editor-polish-and-exporter
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - src/utils/playwrightExporter.ts
  - src/utils/playwrightExporter.test.ts
  - src/components/editor/PlaywrightExportModal.tsx
  - src/components/editor/YamlEditor.tsx
  - src/a11y/en.json
autonomous: true
requirements:
  - EDIT-03

must_haves:
  truths:
    - "playwrightExporter translates all supported YAML step types into clean, idiomatic Playwright TypeScript code"
    - "PlaywrightExportModal provides syntax-highlighted code preview with One-Click Copy and Download .spec.ts buttons"
    - "All text strings flow through en.json via useTranslation"
---

<objective>
Build the `playwrightExporter.ts` code generator, unit test suite, and `PlaywrightExportModal.tsx` export dialog for exporting flows as standalone Playwright TypeScript test specs.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Playwright TypeScript code generator and tests</name>
  <files>src/utils/playwrightExporter.ts, src/utils/playwrightExporter.test.ts</files>
  <action>
    1. In `src/utils/playwrightExporter.ts`, implement `exportFlowToPlaywrightTs(flow: FlowFile, targetUrl?: string): string` mapping actions (`navigate`, `click`, `fill`, `hover`, `press`, `scroll`, `waitFor`, `assertVisible`, `assertText`, `assertTitle`, `assertUrl`, `screenshot`) to `await page.goto()`, `await page.locator().click()`, `await expect()`, etc.
    2. Write unit tests in `src/utils/playwrightExporter.test.ts` testing code generation for all action types and assertions.
  </action>
  <verify>
    <automated>pnpm test src/utils/playwrightExporter.test.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Build PlaywrightExportModal component and trigger in YamlEditor</name>
  <files>src/components/editor/PlaywrightExportModal.tsx, src/components/editor/YamlEditor.tsx, src/a11y/en.json</files>
  <action>
    1. Build `src/components/editor/PlaywrightExportModal.tsx` using `Modal.tsx` showing formatted TypeScript code with line numbers, "Copy Code" button (with feedback checkmark), and "Download .spec.ts" button.
    2. In `src/components/editor/YamlEditor.tsx`, add an "Export Playwright Spec" button (`FileCode2` icon) in the toolbar.
    3. Add translations in `src/a11y/en.json` under `editor.exportPlaywright`, `editor.downloadSpec`, `editor.copiedPlaywright`.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }</automated>
  </verify>
</task>

</tasks>

<output>
Create `.planning/phases/06-editor-polish-and-exporter/06-03-SUMMARY.md` on completion.
</output>
