---
phase: 07-ai-recipes-and-diff-preview
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/qaRecipes.ts
  - src/components/ai/QaRecipeSelector.tsx
  - src/components/ai/AiCopilot.tsx
  - src/a11y/en.json
autonomous: true
requirements:
  - AI-01

must_haves:
  truths:
    - "Users can select from pre-engineered QA recipe presets in AI Copilot"
    - "Selecting a recipe populates prompt with tailored testing instructions and context"
    - "All text strings flow through en.json via useTranslation"
---

<objective>
Build the QA Recipe presets data structure (`src/data/qaRecipes.ts`), `QaRecipeSelector.tsx` chip selector, and integrate into `AiCopilot.tsx`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Define QA recipes and translation keys</name>
  <files>src/data/qaRecipes.ts, src/a11y/en.json</files>
  <action>
    1. Create `src/data/qaRecipes.ts` with categorized QA recipes:
       - Form Validation & Error States
       - Responsive Navigation & Mobile Menu
       - Accessibility & ARIA Assertion Audit
       - Auth & Session Edge Cases
       - E-Commerce Checkout & Payment Flow
    2. Add translations in `src/a11y/en.json` under `copilot.recipes.*`.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Build QaRecipeSelector component and integrate in AiCopilot</name>
  <files>src/components/ai/QaRecipeSelector.tsx, src/components/ai/AiCopilot.tsx</files>
  <action>
    1. Create `src/components/ai/QaRecipeSelector.tsx` displaying scrollable recipe chips with icons and tooltips.
    2. In `src/components/ai/AiCopilot.tsx`, render `QaRecipeSelector` right above the prompt input form.
    3. Clicking a recipe sets the prompt text with the recipe template.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }</automated>
  </verify>
</task>

</tasks>

<output>
Create `.planning/phases/07-ai-recipes-and-diff-preview/07-01-SUMMARY.md` on completion.
</output>
