---
phase: 19-route-topology-and-coverage-visualizer-studio
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - src/components/visualizer/RouteNode.tsx
  - src/components/visualizer/RouteEdge.tsx
  - src/components/visualizer/RouteNode.test.tsx
  - src/components/visualizer/RouteCanvas.tsx
  - src/components/visualizer/RouteCanvas.test.tsx
  - src/components/visualizer/NodeActionModal.tsx
  - src/components/visualizer/CrawlerControlOverlay.tsx
  - src/components/visualizer/CrawlerControlOverlay.test.tsx
  - src/components/visualizer/RouteVisualizerView.tsx
  - src/components/studio/StudioTabs.tsx
  - src/components/studio/StudioRightSidebar.tsx
  - src/components/studio/StudioView.tsx
  - src/a11y/en.json
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
  - VIS-04
must_haves:
  truths:
    - "Interactive @xyflow/react canvas renders discovered routes as customized nodes and action edges with smooth pan/zoom/layout (VIS-01)"
    - "Route nodes visually display color-coded coverage indicators (green: covered, amber: partial, slate: unvisited) and match count badges (VIS-02)"
    - "Clicking a route node or edge opens NodeActionModal allowing one-click YAML flow synthesis or direct flow execution (VIS-03)"
    - "CrawlerControlOverlay displays live crawl stream metrics: active URL, crawler worker position, queue size, discovered routes counter, start/stop controls (VIS-04)"
    - "Route visualizer view is integrated into Studio tab switchboard and responds to active project changes (VIS-01)"
  artifacts:
    - path: "src/components/visualizer/RouteNode.tsx"
      provides: "Custom React Flow node component rendering route metadata, coverage badge, DOM skeleton tag count, and active worker indicator"
      exports: ["RouteNode"]
    - path: "src/components/visualizer/RouteEdge.tsx"
      provides: "Custom React Flow edge component rendering interaction type (click, fill_submit, navigate) with trigger labels"
      exports: ["RouteEdge"]
    - path: "src/components/visualizer/RouteCanvas.tsx"
      provides: "Full interactive XYFlow canvas with mini-map, controls, background grid, and node selection handlers"
      exports: ["RouteCanvas"]
    - path: "src/components/visualizer/NodeActionModal.tsx"
      provides: "Popover/modal for generating YAML flows or executing matching journeys from a clicked route node"
      exports: ["NodeActionModal"]
    - path: "src/components/visualizer/CrawlerControlOverlay.tsx"
      provides: "Floating progress banner and crawl controller with live metrics, origin boundary toggle, and depth inputs"
      exports: ["CrawlerControlOverlay"]
    - path: "src/components/visualizer/RouteVisualizerView.tsx"
      provides: "Main container combining canvas, control overlay, action modal, and empty state guidance"
      exports: ["RouteVisualizerView"]
  key_links:
    - from: "src/components/visualizer/RouteCanvas.tsx"
      to: "src/stores/crawlerStore.ts"
      via: "useCrawlerStore for nodes, edges, selection"
      pattern: "useCrawlerStore"
    - from: "src/components/visualizer/NodeActionModal.tsx"
      to: "src/stores/projectStore.ts"
      via: "addFlow and setActiveFlow actions when generating/executing flows"
      pattern: "useProjectStore"
    - from: "src/components/studio/StudioTabs.tsx"
      to: "src/components/visualizer/RouteVisualizerView.tsx"
      via: "ActiveTab switch to visualizer"
      pattern: "visualizer"
---

<objective>
Build the interactive React Flow Route Topology & Coverage Visualizer Studio UI: custom nodes with coverage badges, action edges, node interaction modal for one-click YAML flow creation/execution, live crawl control overlay, and Studio tab switchboard integration.

Purpose: Deliver an intuitive, visual application topology graph linking site architecture to Playwright test coverage and automated flow generation.
Output: Complete visualizer component suite in `src/components/visualizer/`, Studio tab wiring, test files, and full translation coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/19-route-topology-and-coverage-visualizer-studio/19-01-PLAN.md
@src/types/crawler.ts
@src/stores/crawlerStore.ts
@src/stores/projectStore.ts
@src/components/studio/StudioTabs.tsx
@src/components/studio/StudioView.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Custom RouteNode, RouteEdge, and RouteCanvas with coverage styling (VIS-01, VIS-02)</name>
  <files>src/components/visualizer/RouteNode.tsx, src/components/visualizer/RouteEdge.tsx, src/components/visualizer/RouteCanvas.tsx, src/components/visualizer/RouteNode.test.tsx, src/components/visualizer/RouteCanvas.test.tsx</files>
  <behavior>
    - Test 1: RouteNode renders pathname, coverage badge (green for covered, amber for partial, slate for unvisited), interactive element count, and active target ring when current crawl position matches.
    - Test 2: RouteEdge displays action label (click/navigate/submit) and trigger text.
    - Test 3: RouteCanvas maps crawlerStore nodes/edges into XYFlow format with dagre/hierarchical auto-layout and invokes onNodeClick.
  </behavior>
  <action>
1. Create `src/components/visualizer/RouteNode.tsx`:
   - React Flow custom node taking `NodeProps<VisualizerNodeData>`.
   - Visual styling:
     - Outer container: rounded-lg border bg-stone-900 shadow-md p-3 min-w-[200px] max-w-[280px] font-mono text-xs.
     - Coverage indicator pill:
       - `covered`: `bg-emerald-950/80 border-emerald-500/80 text-emerald-300` ("Covered ({count} flows)")
       - `partial`: `bg-amber-950/80 border-amber-500/80 text-amber-300` ("Partial ({count} flows)")
       - `unvisited`: `bg-stone-950/80 border-stone-700/80 text-stone-400` ("Uncovered")
     - Header: Pathname truncated with tooltip for full URL.
     - Body: Title, interactive elements count (`{count} interactives`), and DOM skeleton hash badge.
     - Active worker indicator: Pulsing cyan border when `isCurrentCrawlTarget` is true.
     - React Flow `Handle` points: `type="target"` top, `type="source"` bottom.
2. Create `src/components/visualizer/RouteEdge.tsx`:
   - React Flow custom edge taking `EdgeProps<VisualizerEdgeData>`.
   - SVG bezier/smoothstep curve with color-coded edge pill:
     - `click`: amber pill with pointer icon
     - `navigate`: cyan pill with link icon
     - `fill_submit`: purple pill with form icon
   - Edge label displaying `triggerText` or `actionType`.
3. Create `src/components/visualizer/RouteCanvas.tsx`:
   - Wraps `@xyflow/react` `<ReactFlow>` with `Background` (dots), `Controls`, and `MiniMap`.
   - Auto-layout helper: Computes (x, y) coordinates using BFS layer rank or standard grid positioning if positions are not pre-set.
   - Registers custom `nodeTypes={{ routeNode: RouteNode }}` and `edgeTypes={{ routeEdge: RouteEdge }}`.
   - Handlers for `onNodeClick`, `onEdgeClick`, and canvas zoom-to-fit on crawl completion.
4. Write unit tests for `RouteNode` and `RouteCanvas`.
  </action>
  <verify>
    <automated>pnpm test src/components/visualizer/RouteNode.test.tsx src/components/visualizer/RouteCanvas.test.tsx</automated>
  </verify>
  <done>React Flow canvas with custom nodes, edges, coverage indicators, and layout tests passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement NodeActionModal and CrawlerControlOverlay (VIS-03, VIS-04)</name>
  <files>src/components/visualizer/NodeActionModal.tsx, src/components/visualizer/CrawlerControlOverlay.tsx, src/components/visualizer/CrawlerControlOverlay.test.tsx</files>
  <behavior>
    - Test 1: NodeActionModal displays route details, list of matched existing flows with 'Run Now' button, and 'Generate Flow for Route' button.
    - Test 2: Clicking 'Generate Flow for Route' triggers flow compilation, converts DiscoveredFlow to project YAML flow, and opens it in editor.
    - Test 3: CrawlerControlOverlay displays active crawl state, progress metrics (visited/discovered/queue), start crawl inputs (target URL, max pages, depth), and Stop button.
  </behavior>
  <action>
1. Create `src/components/visualizer/NodeActionModal.tsx`:
   - Modal or slide-out drawer triggered when `selectedNodeId` is set in `useCrawlerStore`.
   - Sections:
     - Route Info: Pathname, full URL, DOM skeleton hash, discovery timestamp.
     - Coverage Status: Display status badge and list of existing project flows covering this route with direct "Run Flow" buttons (invoking runner via `executionStore` / `projectStore`).
     - Interactive Elements Summary: List of buttons, links, inputs discovered on this page.
     - Journey Generation: "Generate Test Journey for this Route" button:
       - Calls `crawlerStore.generateFlowsForRoute(nodeId)`.
       - Emits synthesized YAML flow into `projectStore.addFlow()`.
       - Sets newly generated flow as active and switches tab or provides toast confirmation.
2. Create `src/components/visualizer/CrawlerControlOverlay.tsx`:
   - Floating header/overlay positioned on top of the canvas:
     - Idle state: Target URL input (pre-populated with active project URL), Max Pages input, Max Depth slider, "Start Autonomous Crawl" button with sparkle icon.
     - Crawling state:
       - Live ticker: Active URL being probed (`currentUrl`).
       - Metric chips: Visited Pages (`{visited}/{discovered}`), Queue Size (`{queue}`), Phase badge (`discovering` | `interacting` | `submitting`).
       - "Stop Crawl" button with pause/stop icon.
     - Error banner if crawler encounters origin mismatch or network failure.
3. Write unit tests in `src/components/visualizer/CrawlerControlOverlay.test.tsx`.
  </action>
  <verify>
    <automated>pnpm test src/components/visualizer/CrawlerControlOverlay.test.tsx</automated>
  </verify>
  <done>Node action modal and live crawl control overlay operational with verified test coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate RouteVisualizerView into Studio switchboard and finalize i18n (VIS-01, VIS-02, VIS-03, VIS-04)</name>
  <files>src/components/visualizer/RouteVisualizerView.tsx, src/components/studio/StudioTabs.tsx, src/components/studio/StudioRightSidebar.tsx, src/components/studio/StudioView.tsx, src/a11y/en.json</files>
  <behavior>
    - Test 1: StudioTabs renders Visualizer tab button with Network/Compass icon.
    - Test 2: Switching to 'visualizer' tab displays RouteVisualizerView containing canvas, overlay, and empty state guide when no crawl has run.
    - Test 3: All visualizer and crawler UI strings pass through useTranslation() with zero hardcoded English text.
  </behavior>
  <action>
1. Create `src/components/visualizer/RouteVisualizerView.tsx`:
   - Combines `RouteCanvas`, `CrawlerControlOverlay`, `NodeActionModal`.
   - Handles empty state: When `nodes.length === 0`, render informative visual card explaining autonomous crawl benefits with one-click "Start Crawl on {activeProject.url}" CTA.
   - Syncs project flows with `crawlerStore.syncCoverageWithFlows(activeProject.flows)` on mount and when project flows change.
2. Update `src/components/studio/StudioTabs.tsx`:
   - Add `{ id: 'visualizer', labelKey: 'studio.tabVisualizer', icon: Network }` to `TABS` list.
3. Update `src/components/studio/StudioRightSidebar.tsx` / `StudioView.tsx`:
   - Wire `'visualizer'` tab rendering: when `activeTab === 'visualizer'`, render `<RouteVisualizerView />` filling the main content viewport or dedicated tab panel.
4. Ensure all user-facing strings are strictly extracted into `src/a11y/en.json` under `"visualizer"` and `"crawler"` keys.
5. Run full test suite and linter to verify clean build.
  </action>
  <verify>
    <automated>pnpm lint && pnpm test</automated>
  </verify>
  <done>Visualizer fully integrated into Studio, zero hardcoded strings, all lint and unit tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Flow Generation -> Project Disk Files | Generating flows from crawler graph writes new YAML files to local disk via projectStore. |
| URL Input -> Crawl Target | User can input arbitrary URLs to begin crawl exploration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Cross-Site Traversal / Boundary Escape | `CrawlerControlOverlay.tsx` | mitigate | Default crawl target to active project URL and enforce origin boundary constraint by default. |
| T-19-04 | Overwriting Existing Flows | `NodeActionModal.tsx` | mitigate | Generated flow names include route slug and timestamp or counter; never overwrite existing flows without user confirmation. |
</threat_model>

<verification>
- `pnpm lint` passes with 0 warnings and 0 errors
- `pnpm test` passes all unit and component tests
- `pnpm typecheck` passes cleanly
</verification>

<success_criteria>
Interactive @xyflow/react canvas renders topology and coverage, node action modal enables flow generation and execution, live crawler overlay streams crawl progress, and Studio tab integrates seamlessly.
</success_criteria>

<output>
After completion, create `.planning/phases/19-route-topology-and-coverage-visualizer-studio/19-02-SUMMARY.md`
</output>
