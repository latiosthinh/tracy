---
phase: 19-route-topology-and-coverage-visualizer-studio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/types/crawler.ts
  - src/types/ui.ts
  - src/stores/crawlerStore.ts
  - src/stores/crawlerStore.test.ts
  - src/a11y/en.json
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-04
must_haves:
  truths:
    - "@xyflow/react is installed and available for React 19 visualizer canvas (VIS-01)"
    - "Zustand crawlerStore tracks crawl graph nodes, edges, active crawl status, progress events, and route coverage calculation (VIS-01, VIS-02, VIS-04)"
    - "Crawl progress stream updates active node position, queue size, discovered pages count, and errors in real time (VIS-04)"
    - "Coverage metrics accurately evaluate routes as covered, partial, or unvisited based on existing flows in active project (VIS-02)"
  artifacts:
    - path: "src/types/crawler.ts"
      provides: "Frontend crawler and route graph data types, node status types, coverage calculation contracts"
      exports: ["RouteCoverageStatus", "VisualizerNodeData", "VisualizerEdgeData", "CrawlerStoreState"]
    - path: "src/stores/crawlerStore.ts"
      provides: "Reactive Zustand store managing route graph, live crawler execution, coverage mapping, IPC event listener"
      exports: ["useCrawlerStore", "calculateRouteCoverage"]
  key_links:
    - from: "src/stores/crawlerStore.ts"
      to: "src/lib/ipc.ts"
      via: "tracyApi crawler methods and event listeners"
      pattern: "tracyApi\\.(startCrawl|stopCrawl|onCrawlerProgress|generateCrawlFlows)"
    - from: "src/stores/crawlerStore.ts"
      to: "src/types/crawler.ts"
      via: "Type imports for VisualizerNodeData and state"
      pattern: "import.*VisualizerNodeData.*from"
---

<objective>
Install `@xyflow/react`, define crawler/visualizer TypeScript contracts, and build the `crawlerStore` Zustand state manager with live crawl IPC stream ingestion and automatic route coverage computation.

Purpose: Provide reactive state, real-time IPC synchronization, and coverage status evaluation for the Route Topology & Coverage Visualizer.
Output: `@xyflow/react` installed, `src/types/crawler.ts`, `src/stores/crawlerStore.ts`, test suite, updated `en.json`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@electron/core/crawler/types.ts
@src/lib/ipc.ts
@src/stores/projectStore.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install @xyflow/react and create crawler types & coverage contracts (VIS-01, VIS-02)</name>
  <files>package.json, src/types/crawler.ts, src/types/ui.ts, src/a11y/en.json</files>
  <behavior>
    - Test 1: @xyflow/react imports successfully in React 19 environment.
    - Test 2: src/types/crawler.ts exports RouteCoverageStatus ('covered' | 'partial' | 'unvisited'), VisualizerNodeData, VisualizerEdgeData, and FlowGenerationOptions.
    - Test 3: src/types/ui.ts includes 'visualizer' in ActiveTab union type.
  </behavior>
  <action>
1. Run `pnpm add @xyflow/react` in `QA/tracy/` (or update `package.json` with `@xyflow/react: "^12.4.0"` and run pnpm install).
2. Create `src/types/crawler.ts`:
   - Re-export or mirror `CrawlNode`, `CrawlEdge`, `CrawlProgressEvent`, `DiscoveredFlow` compatible with `electron/core/crawler/types.ts`.
   - Define `RouteCoverageStatus = 'covered' | 'partial' | 'unvisited'`.
   - Define `VisualizerNodeData`:
     ```typescript
     export interface VisualizerNodeData {
       label: string;
       url: string;
       pathname: string;
       title?: string;
       skeletonHash: string;
       coverageStatus: RouteCoverageStatus;
       matchedFlowNames: string[];
       interactiveCount: number;
       isCurrentCrawlTarget?: boolean;
       isTerminal?: boolean;
       visitedAt?: number;
     }
     ```
   - Define `VisualizerEdgeData`:
     ```typescript
     export interface VisualizerEdgeData {
       actionType: 'click' | 'navigate' | 'fill_submit';
       selector?: string;
       triggerText?: string;
       isTraversed?: boolean;
     }
     ```
   - Define `CrawlerStoreState` contract with graph, progress, running state, and action dispatchers.
3. Update `src/types/ui.ts` to include `'visualizer'` in `ActiveTab`:
   `export type ActiveTab = 'editor' | 'steps' | 'timeline' | 'ai' | 'reports' | 'cli' | 'config' | 'visualizer';`
4. Add visualizer and crawler translation keys to `src/a11y/en.json` under `"visualizer"` and `"crawler"` sections.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>Dependencies installed, contracts defined without TypeScript errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build Zustand crawlerStore with IPC progress streaming and coverage calculations (VIS-02, VIS-04)</name>
  <files>src/stores/crawlerStore.ts, src/stores/crawlerStore.test.ts</files>
  <behavior>
    - Test 1: calculateRouteCoverage accurately tags nodes as 'covered' if an existing flow starts/visits that pathname, 'partial' if flow matches domain/prefix but not exact actions, or 'unvisited' if no flow touches it.
    - Test 2: startCrawl sets isCrawling=true, clears previous errors, calls tracyApi.startCrawl, and initializes progress listener.
    - Test 3: onCrawlerProgress listener updates totalDiscovered, totalVisited, queueLength, currentUrl, and adds incoming nodes/edges to graph incrementally.
    - Test 4: stopCrawl invokes tracyApi.stopCrawl and resets isCrawling=false.
    - Test 5: generateFlows calls tracyApi.generateCrawlFlows and returns synthesized DiscoveredFlows.
  </behavior>
  <action>
1. Create `src/stores/crawlerStore.ts`:
   - Implement pure helper `calculateRouteCoverage(pathname: string, url: string, existingFlows: Flow[]): { status: RouteCoverageStatus; matchedFlowNames: string[] }`:
     - Inspect all flows in project: check if flow has steps navigating to or interacting with `pathname` or `url`.
     - Return `'covered'` if at least one flow contains explicit steps on this pathname.
     - Return `'partial'` if a flow navigates to the root or sibling path.
     - Return `'unvisited'` otherwise.
   - Implement Zustand store `useCrawlerStore`:
     - State: `nodes: CrawlNode[]`, `edges: CrawlEdge[]`, `isCrawling: boolean`, `progress: CrawlProgressEvent | null`, `error: string | null`, `selectedNodeId: string | null`, `selectedEdgeId: string | null`, `discoveredFlows: DiscoveredFlow[]`.
     - Actions:
       - `startCrawl(startUrl: string, options?: CrawlOptions)`
       - `stopCrawl()`
       - `handleProgressEvent(event: CrawlProgressEvent)`
       - `addNode(node: CrawlNode)`
       - `addEdge(edge: CrawlEdge)`
       - `setSelectedNode(nodeId: string | null)`
       - `setSelectedEdge(edgeId: string | null)`
       - `generateFlowsForRoute(nodeId: string)`
       - `resetGraph()`
       - `syncCoverageWithFlows(flows: Flow[])`
     - Auto-wire `tracyApi.onCrawlerProgress` subscription when `startCrawl` is executed, storing `unlisten` cleanup reference.
2. Write unit tests in `src/stores/crawlerStore.test.ts` mocking `tracyApi` and testing state transitions, coverage derivation, and progress handling.
  </action>
  <verify>
    <automated>pnpm test src/stores/crawlerStore.test.ts</automated>
  </verify>
  <done>Zustand crawler store manages graph state, IPC streaming, and coverage computation with 100% test pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IPC Stream -> Zustand Store | Crawler progress events and graph nodes received from Electron main process. |
| User Actions -> Main Process IPC | Start/stop crawl commands dispatched from UI to main process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Denial of Service (Memory Bloat) | `crawlerStore.ts` | mitigate | Deduplicate nodes by `id` (url + skeletonHash) and limit max graph display count to 500 nodes. |
| T-19-02 | State Desynchronization | `crawlerStore.ts` | mitigate | Clean up IPC `unlisten` callback on stopCrawl and component unmount to prevent memory leaks and zombie listeners. |
</threat_model>

<verification>
- `pnpm test src/stores/crawlerStore.test.ts` passes
- `pnpm typecheck` passes with zero errors
</verification>

<success_criteria>
@xyflow/react installed, crawler store created, live IPC progress subscription operating, and route coverage calculation verified.
</success_criteria>

<output>
After completion, create `.planning/phases/19-route-topology-and-coverage-visualizer-studio/19-01-SUMMARY.md`
</output>
