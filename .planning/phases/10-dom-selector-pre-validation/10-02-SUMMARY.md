---
phase: 10-dom-selector-pre-validation
plan: 02
subsystem: selector-scoring
tags: [dom-validation, selector-scoring, ambiguity-tagging, resilience]
requires:
  - 10-01
provides:
  - Selector stability classification into UniquePresent, AmbiguousMultiple, NotPresent, and DeferredDynamic
  - Locator resilience ranking hierarchy: TestId > AriaRole > VisibleText > StandardCss > DeepXPath
  - Automatic suggestion generator from probed element attributes
affects:
  - src/types/skills.ts
  - src/types/index.ts
  - src/utils/selectorScorer.ts
  - src/utils/selectorScorer.test.ts
tech-stack:
  added: []
  patterns:
    - Bounded scoring with fragility and ambiguity penalties
    - Dynamic predecessor step analysis for deferred visibility
    - Fast regex detection for autogenerated IDs and hashed CSS classes
key-files:
  created:
    - src/utils/selectorScorer.ts
    - src/utils/selectorScorer.test.ts
  modified:
    - src/types/skills.ts
    - src/types/index.ts
decisions:
  - Base stability tier score mapped from 100 (TestId) down to 20 (DeepXPath)
  - Applied penalty deductions for autogenerated classes (-25), ambiguous matches (-30), and invisibility (-40)
  - Flags DeferredDynamic when zero matches occur following interactive modal/menu trigger steps
metrics:
  duration: 3m
  completed_date: "2026-08-19"
---

# Phase 10 Plan 02: DOM Selector Stability Scoring & Ambiguity Classifier Summary

Implemented selector stability scoring, presence status classification (`UniquePresent`, `AmbiguousMultiple`, `NotPresent`, `DeferredDynamic`), and candidate resilience ranking in `src/utils/selectorScorer.ts` fulfilling requirement VERIFY-02.

## Deliverables & Key Changes

1. **Contracts & Types (`src/types/skills.ts`, `src/types/index.ts`)**:
   - Declared `SelectorPresenceStatus`, `SelectorResilienceTier`, `SelectorScoreReport`, and `PrecedingStepContext`.

2. **Scoring & Heuristic Classifier (`src/utils/selectorScorer.ts`)**:
   - `detectResilienceTier`: maps selectors to TestId (100), AriaRole (85), VisibleText (70), StandardCss (50), or DeepXPath (20).
   - `isAutoGeneratedSelector`: detects CSS-in-JS hashes, Ember IDs, and random numeric identifiers.
   - `classifyPresenceStatus`: distinguishes unique matches, ambiguous locators, missing elements, and deferred dynamic components.
   - `scoreSelectorResilience`: computes 0-100 bounded score, warnings, suggestions, and reliability flags.
   - `rankSelectorsByResilience`: sorts candidates in descending order of stability for downstream multi-provider generator loops.

3. **Unit Tests (`src/utils/selectorScorer.test.ts`)**:
   - Comprehensive test suite covering all tiers, statuses, penalties, suggestions, and ranking.

## Verification

- `pnpm lint` passed cleanly (zero errors, zero warnings).
- `pnpm typecheck` passed cleanly.
- `pnpm test` passed (34 test files, 394 tests).

## Deviations from Plan

None - plan executed exactly as written.

## Self-Check: PASSED
- `src/utils/selectorScorer.ts` exists and tested
- `src/utils/selectorScorer.test.ts` exists with 14 passing tests
- Commits `3b0fc87` and `8dba159` verified in git log
