---
phase: 03-hardening
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/hooks/useAutoSave.ts
  - src/stores/uiStore.ts
  - src/stores/projectStore.ts
  - src/components/settings/SettingsModal.tsx
  - src/stores/executionStore.ts
  - src/components/layout/AppShell.tsx
  - src/lib/ipc.ts
  - electron/ipc/playwrightEngine.ts
  - electron/ipc/fileSystem.ts
  - src/components/ui/Modal.tsx
  - src/components/shared/SplashScreen.tsx
autonomous: true
requirements:
  - FIX-01
  - FIX-02
  - FIX-03
  - FIX-04

must_haves:
  truths:
    - "Auto-save is wired to a single persisted store setting (uiStore.defaultSaveLocation) and serializes DOM snapshots from domSnapshotStore"
    - "IPC event listeners in executionStore are deduplicated with synchronous pending checks and AppShell cleans them up"
    - "All onX event wrappers in ipc.ts safely return noop unlisten functions in non-Electron environments"
    - "playwrightEngine does not invoke ESM-illegal require('electron') and guards sender.isDestroyed"
    - "Modal.tsx sets initial focus, traps focus without letting Tab escape, locks body scroll, and restores trigger focus on close"
    - "Execution pause uses a generation token and never produces PASSED status on incomplete runs"
---

<objective>
Remediate core correctness findings:
1. F1 Auto-save wiring: single source of truth in `uiStore`, SettingsModal writes it, `useAutoSave` reads it and serializes live snapshots from `domSnapshotStore`.
2. F2 Listener dedupe: synchronous pending guard in `setupEventListeners` and AppShell cleanup.
3. F5 Web-mode IPC guards: add `isElectronEnv` checks to `onAgentStreamChunk`, `onStepUpdate`, and `onExecutionLog`.
4. F3 Engine crash: remove `require('electron')` in `playwrightEngine.ts` and guard `sender.isDestroyed()`.
5. F6 Modal focus trap: complete WAI-ARIA focus trap with initial focus, boundary wrap, `:not(:disabled)` selector, scroll lock, and trigger focus restore.
6. F7 Pause semantics: run generation token to prevent interleaved logs and false `PASSED` status on paused runs.
7. Splash & boot sync: gate splash fade on `!dataReady` and prevent WelcomeSetup flash.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Wire auto-save and DOM snapshots serialization</name>
  <files>src/hooks/useAutoSave.ts, src/stores/uiStore.ts, src/stores/projectStore.ts, src/components/settings/SettingsModal.tsx</files>
  <action>
    1. In `src/stores/uiStore.ts`, persist `defaultSaveLocation` and provide `setDefaultSaveLocation`.
    2. In `src/components/settings/SettingsModal.tsx`, connect the default save location input to `useUiStore`'s `setDefaultSaveLocation`.
    3. In `src/hooks/useAutoSave.ts`, read `defaultSaveLocation` from `useUiStore` and serialize snapshots from `useDomSnapshotStore.getState().getAllDomSnapshots(projectId)`.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Listener deduplication, web-mode IPC guards, and engine crash fix</name>
  <files>src/stores/executionStore.ts, src/components/layout/AppShell.tsx, src/lib/ipc.ts, electron/ipc/playwrightEngine.ts, electron/ipc/fileSystem.ts</files>
  <action>
    1. In `src/stores/executionStore.ts`, set synchronous `eventListenersSetting = true` flag before async calls and unlisten any prior listeners before attaching new ones.
    2. In `src/components/layout/AppShell.tsx`, return `() => cleanupEventListeners()` in the boot effect.
    3. In `src/lib/ipc.ts`, add `if (!isElectronEnv()) return () => {};` to `onAgentStreamChunk`, `onStepUpdate`, and `onExecutionLog`.
    4. In `electron/ipc/playwrightEngine.ts`, capture `BrowserWindow.fromWebContents(event.sender)` in handler registrations instead of `require('electron')`. Guard `!sender.isDestroyed()` before `event.sender.send` calls.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: A11y modal focus trap, pause generation token, and boot synchronization</name>
  <files>src/components/ui/Modal.tsx, src/stores/executionStore.ts, src/components/shared/SplashScreen.tsx, src/stores/projectStore.ts</files>
  <action>
    1. In `src/components/ui/Modal.tsx`, set initial focus to the first interactive element or dialog container. Implement containment check `if (!modalRef.current.contains(document.activeElement))`. Filter out disabled elements (`:not(:disabled)`). Lock body scroll while open and restore previous focus on close.
    2. In `src/stores/executionStore.ts`, introduce `runToken` generation counter. On pause or abort, set status to `PAUSED`/`FAILED` rather than generating a false `PASSED` result.
    3. In `src/stores/projectStore.ts`, ensure `deleteProject` and `closeProjectTab` cleanly sync `openProjectIds` and reset `activeFlowId`.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }</automated>
  </verify>
</task>

</tasks>

<output>
Create `.planning/phases/03-hardening/03-02-SUMMARY.md` on completion.
</output>
