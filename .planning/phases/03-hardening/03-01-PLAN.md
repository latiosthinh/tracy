---
phase: 03-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/ipc/cliRunner.ts
  - electron/ipc/fileSystem.ts
  - electron/main.ts
  - electron/ipc/webviewManager.ts
  - electron/ipc/playwrightEngine.ts
  - electron/ipc/aiProvider.ts
  - electron/ipc/security.test.ts
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03

must_haves:
  truths:
    - "cliRunner quotes and sanitizes prompt arguments when spawning under Windows cmd.exe shims to prevent command injection"
    - "assertSafePath strictly enforces path.sep boundaries and validates saveLocation against allowable base directories"
    - "main.ts blocks unauthorized external window opening and navigation"
    - "All URL-accepting IPC handlers reject non-http/https/about:blank schemes"
    - "All fetch calls in aiProvider.ts have explicit AbortSignal timeouts and handle terminal cursor states"
---

<objective>
Remediate critical security findings identified in audit:
1. S1 Command injection in `cliRunner.ts`: quote and escape arguments for Windows `.cmd` shims, with hostile prompt unit tests.
2. S2 & S3 Path traversal in `fileSystem.ts`: enforce `path.sep` boundary in `assertSafePath` and validate `saveLocation` against allowed base paths.
3. S4 Navigation jail in `main.ts`: add `setWindowOpenHandler` and `will-navigate` jail to the main BrowserWindow and WebContentsViews.
4. S4 URL allowlist everywhere: enforce `isAllowedNavigationUrl` (http/https/about:blank) across `navigate_browser`, `run_flow`, `mine_batch_urls`, and webviews.
5. F4 Timeouts in `aiProvider.ts`: add `AbortSignal.timeout` on all fetches, terminal cursor status handling (`cancelled`, `expired`), and message pagination.
6. M13 Webview input validation: validate `typeof url === 'string'` and numeric finite bounds.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Command injection prevention and path traversal fix with unit tests</name>
  <files>electron/ipc/cliRunner.ts, electron/ipc/fileSystem.ts, electron/ipc/security.test.ts</files>
  <action>
    1. In `electron/ipc/cliRunner.ts`, implement `quoteCmdArg(arg: string): string` to properly escape arguments when `useShell` is true on Windows. Add `child.on('error')` listener to avoid unhandled crashes on bad paths.
    2. In `electron/ipc/fileSystem.ts`, update `assertSafePath`: `resolvedTarget === resolvedBase || resolvedTarget.startsWith(resolvedBase + path.sep)`. Validate `saveLocation` base paths.
    3. Write `electron/ipc/security.test.ts` testing hostile command injection payloads (`"; whoami & calc`, `& dir`) and path traversal bypasses (`flowsEvil/x`, `../`).
  </action>
  <verify>
    <automated>pnpm test electron/ipc/security.test.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Navigation jail and URL allowlist enforcement across main and IPC handlers</name>
  <files>electron/main.ts, electron/ipc/webviewManager.ts, electron/ipc/playwrightEngine.ts</files>
  <action>
    1. In `electron/main.ts`, add `win.webContents.setWindowOpenHandler(() => ({ action: 'deny' }))` and `will-navigate` handler preventing unauthorized external navigations.
    2. In `electron/ipc/webviewManager.ts`, attach `will-navigate` listeners to created `WebContentsView` instances blocking invalid schemes. Add numeric bounds check (`Number.isFinite`) and string type validation.
    3. In `electron/ipc/playwrightEngine.ts`, enforce URL scheme allowlist (`http:`, `https:`, `about:blank`) in `navigate_browser`, `run_flow`, and `mine_batch_urls`.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: aiProvider fetch timeouts and terminal cursor state handling</name>
  <files>electron/ipc/aiProvider.ts</files>
  <action>
    1. Add `signal: AbortSignal.timeout(60_000)` to one-shot generation fetches in OpenAI, Claude, Custom Gateway, and Cursor providers.
    2. Add `signal: AbortSignal.timeout(15_000)` to cursor task poll and message fetches.
    3. In `createCursorApiProvider`, handle `cancelled`, `expired`, `failed` explicitly as terminal error states, and handle message pagination if `has_more` is present.
  </action>
  <verify>
    <automated>pnpm test electron/aiProvider.test.ts</automated>
  </verify>
</task>

</tasks>

<output>
Create `.planning/phases/03-hardening/03-01-SUMMARY.md` on completion.
</output>
