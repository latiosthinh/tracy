---
phase: 02-a11y-and-text-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - eslint.config.js
  - src/a11y/en.json
  - src/hooks/useTranslation.ts
  - src/hooks/useTranslation.test.ts
  - src/components/ui/Modal.tsx
autonomous: true
requirements:
  - A11Y-01
  - A11Y-02
  - A11Y-03

must_haves:
  truths:
    - "useTranslation supports {param} string interpolation and returns fallback gracefully"
    - "src/a11y/en.json has domain hierarchy ready for feature-by-feature text extraction"
    - "eslint-plugin-jsx-a11y is installed and active in ESLint config"
    - "Modal.tsx enforces role='dialog', aria-modal='true', Escape key closing, and focus trapping"
  artifacts:
    - path: "src/hooks/useTranslation.ts"
      provides: "Enhanced translation hook with interpolation"
      contains: "params?: Record<string, string | number>"
    - path: "src/components/ui/Modal.tsx"
      provides: "A11y-hardened modal container with focus trap and Escape handler"
      contains: "aria-modal"
---

<objective>
Build the core a11y & i18n foundation:
1. Install and configure `eslint-plugin-jsx-a11y` in ESLint.
2. Upgrade `src/hooks/useTranslation.ts` with parameter interpolation and add test coverage.
3. Structure `src/a11y/en.json` with domain categories for all app features.
4. Upgrade `src/components/ui/Modal.tsx` with WAI-ARIA dialog semantics, focus trapping, and Escape-to-close.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Install eslint-plugin-jsx-a11y and configure ESLint</name>
  <files>package.json, eslint.config.js</files>
  <action>
    1. Install `eslint-plugin-jsx-a11y` as a devDependency via `pnpm add -D eslint-plugin-jsx-a11y`.
    2. Add jsx-a11y plugin to `eslint.config.js` (or `.eslintrc.cjs` depending on configuration file in repo).
    3. Run `pnpm lint` and ensure baseline passes.
  </action>
  <verify>
    <automated>pnpm lint</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Upgrade useTranslation hook with interpolation and unit tests</name>
  <files>src/hooks/useTranslation.ts, src/hooks/useTranslation.test.ts, src/a11y/en.json</files>
  <action>
    1. Write unit tests in `src/hooks/useTranslation.test.ts` testing nested key lookup, missing key fallback, and `{param}` replacement.
    2. Update `src/hooks/useTranslation.ts` to implement `t(key: string, params?: Record<string, string | number>): string`.
    3. Expand `src/a11y/en.json` top-level domains: `common`, `toolbar`, `header`, `tabs`, `studio`, `copilot`, `settings`, `projects`, `modals`, `reports`, `setup`, `docs`, `domMiner`.
  </action>
  <verify>
    <automated>pnpm test src/hooks/useTranslation.test.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: A11y-harden shared Modal container</name>
  <files>src/components/ui/Modal.tsx</files>
  <action>
    1. In `src/components/ui/Modal.tsx`, add `role="dialog"`, `aria-modal="true"`, and `aria-labelledby`.
    2. Add `keydown` listener for `Escape` to call `onClose()`.
    3. Add focus trap on `Tab` / `Shift+Tab` within the modal reference.
    4. Ensure close button has `aria-label={t('common.close')}`.
  </action>
  <verify>
    <automated>pnpm test && pnpm lint</automated>
  </verify>
</task>

</tasks>

<output>
Create `.planning/phases/02-a11y-and-text-extraction/02-01-SUMMARY.md` on completion.
</output>
