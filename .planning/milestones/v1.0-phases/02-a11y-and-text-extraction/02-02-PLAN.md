---
phase: 02-a11y-and-text-extraction
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/a11y/en.json
  - src/components/layout/Header.tsx
  - src/components/header/BrandLogo.tsx
  - src/components/header/ProjectTabs.tsx
  - src/components/header/FlowTabs.tsx
  - src/components/studio/StudioToolbar.tsx
  - src/components/studio/RealBrowserView.tsx
  - src/components/studio/StepTimeline.tsx
  - src/components/studio/ElementInspector.tsx
  - src/components/studio/DomMinerPanel.tsx
  - src/components/studio/StudioRightSidebar.tsx
  - src/components/studio/StudioTabs.tsx
  - src/components/studio/StudioView.tsx
autonomous: true
requirements:
  - A11Y-01
  - A11Y-03
---

<objective>
Extract all hardcoded text strings into `src/a11y/en.json` and add full a11y attributes across Header, Tabs, and Studio components:
1. `Header.tsx`, `BrandLogo.tsx`, `ProjectTabs.tsx`, `FlowTabs.tsx`
2. `StudioToolbar.tsx`, `RealBrowserView.tsx`, `StepTimeline.tsx`, `ElementInspector.tsx`, `DomMinerPanel.tsx`, `StudioRightSidebar.tsx`, `StudioTabs.tsx`, `StudioView.tsx`
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Extract strings and add a11y to Header, BrandLogo, and Tab components</name>
  <files>src/a11y/en.json, src/components/layout/Header.tsx, src/components/header/BrandLogo.tsx, src/components/header/ProjectTabs.tsx, src/components/header/FlowTabs.tsx</files>
  <action>
    1. Populate `header` and `tabs` sections in `src/a11y/en.json`.
    2. Replace all hardcoded JSX text, button titles, and aria-labels in `Header.tsx`, `BrandLogo.tsx`, `ProjectTabs.tsx`, and `FlowTabs.tsx` with `t()`.
    3. Ensure decorative icons have `aria-hidden="true"` and icon-only buttons have matching `aria-label` and `title`.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Extract strings and add a11y to Studio & Browser components</name>
  <files>src/a11y/en.json, src/components/studio/StudioToolbar.tsx, src/components/studio/RealBrowserView.tsx, src/components/studio/StepTimeline.tsx, src/components/studio/ElementInspector.tsx, src/components/studio/DomMinerPanel.tsx, src/components/studio/StudioRightSidebar.tsx, src/components/studio/StudioTabs.tsx, src/components/studio/StudioView.tsx</files>
  <action>
    1. Populate `studio`, `toolbar`, and `domMiner` sections in `src/a11y/en.json`.
    2. Replace all text, tooltips, placeholders, and status labels with `t()`.
    3. Add `aria-live="polite"` on timeline log updates and live step runners.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }</automated>
  </verify>
</task>

</tasks>

<output>
Create `.planning/phases/02-a11y-and-text-extraction/02-02-SUMMARY.md` on completion.
</output>
