---
phase: 02-a11y-and-text-extraction
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/a11y/en.json
  - src/components/settings/SettingsModal.tsx
  - src/components/settings/UiSettingsPanel.tsx
  - src/components/setup/WelcomeSetup.tsx
  - src/components/projects/ProjectManager.tsx
  - src/components/projects/ProjectManagerModal.tsx
  - src/components/projects/ExportImportPanel.tsx
  - src/components/shared/AgentSelector.tsx
  - src/components/shared/DocsModal.tsx
  - src/components/studio/BatchMinerModal.tsx
  - src/components/editor/CreateFlowModal.tsx
  - src/components/shared/ErrorBoundary.tsx
autonomous: true
requirements:
  - A11Y-01
  - A11Y-03
---

<objective>
Extract all hardcoded text and add full accessible semantics across Settings, Setup, Projects, and Modal dialogs:
1. `SettingsModal.tsx`, `UiSettingsPanel.tsx`, `AgentSelector.tsx`
2. `WelcomeSetup.tsx`, `ProjectManager.tsx`, `ProjectManagerModal.tsx`, `ExportImportPanel.tsx`
3. `DocsModal.tsx`, `BatchMinerModal.tsx`, `CreateFlowModal.tsx`, `ErrorBoundary.tsx`
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Extract strings and add a11y to Settings, Setup, and AgentSelector</name>
  <files>src/a11y/en.json, src/components/settings/SettingsModal.tsx, src/components/settings/UiSettingsPanel.tsx, src/components/setup/WelcomeSetup.tsx, src/components/shared/AgentSelector.tsx</files>
  <action>
    1. Populate `settings` and `setup` keys in `src/a11y/en.json`.
    2. Extract all strings, descriptions, tab labels, form input placeholders, and connection test messages.
    3. Ensure inputs have explicit `<label htmlFor="...">` and radio/button options have appropriate aria roles.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Extract strings and add a11y to Project Management & Modals</name>
  <files>src/a11y/en.json, src/components/projects/ProjectManager.tsx, src/components/projects/ProjectManagerModal.tsx, src/components/projects/ExportImportPanel.tsx, src/components/shared/DocsModal.tsx, src/components/studio/BatchMinerModal.tsx, src/components/editor/CreateFlowModal.tsx, src/components/shared/ErrorBoundary.tsx</files>
  <action>
    1. Populate `projects`, `modals`, and `docs` in `src/a11y/en.json`.
    2. Replace all headings, form labels, tooltips, validation messages, and buttons with `t()`.
    3. Ensure all modal dialogs have `aria-labelledby` linking to their respective modal title headers.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }</automated>
  </verify>
</task>

</tasks>

<output>
Create `.planning/phases/02-a11y-and-text-extraction/02-03-SUMMARY.md` on completion.
</output>
