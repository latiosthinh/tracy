---
phase: 03-hardening
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - eslint.config.mjs
  - package.json
  - src/a11y/a11yTextGuard.test.ts
  - src/a11y/en.json
  - src/hooks/useTranslation.ts
  - src/hooks/useTranslation.test.ts
  - src/utils/flowUtils.ts
  - src/components/editor/YamlEditor.tsx
  - src/components/settings/SettingsModal.tsx
  - src/components/projects/ProjectManager.tsx
  - src/components/studio/BatchMinerModal.tsx
  - src/components/projects/ExportImportPanel.tsx
  - src/components/shared/AgentSelector.tsx
  - src/components/ui/IconButton.tsx
autonomous: true
requirements:
  - CIX-01
  - CIX-02

must_haves:
  truths:
    - "ESLint config enforces unused-imports as errors, typescript-eslint rules, react-hooks rules, and jsx-a11y interactive rules at error level"
    - "pnpm lint runs with --max-warnings 0 and passes cleanly"
    - "a11yTextGuard test scans JSX text with punctuation, attributes (aria-label/title/placeholder), expression literals, and utils directories"
    - "All remaining ~59 hardcoded UI strings are extracted to en.json and duplicate keys are deduped into common.*"
    - "useTranslation key parameter is typed against the dictionary dot-paths"
---

<objective>
Restore strict CI quality gates and complete text extraction:
1. G2 Restore ESLint configuration: `unused-imports` error, `typescript-eslint` recommended, `react-hooks` recommended, `jsx-a11y` interactive rules at error level, `--max-warnings 0` in lint script.
2. G1 Rebuild `a11yTextGuard.test.ts`: scan punctuation (`:`, `(`, `)`, `&`, `/`), attribute strings, JSX expression literals, and `src/utils/`.
3. Extract all remaining ~59 hardcoded strings (YamlEditor autocomplete descriptions, SettingsModal skills list, flowUtils categories, straggling aria-labels).
4. Dedupe `en.json` keys to `common.*`, move `test.greeting` fixture out of production dictionary, and interpolate `splash.versionInfo`.
5. Type `useTranslation` key parameter with dot-path union.
6. Add `aria-label` to `IconButton`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Restore strict ESLint configuration and typed useTranslation keys</name>
  <files>eslint.config.mjs, package.json, src/hooks/useTranslation.ts, src/hooks/useTranslation.test.ts</files>
  <action>
    1. In `eslint.config.mjs`, configure typescript-eslint parser, unused-imports plugin (error), react-hooks plugin (recommended), and jsx-a11y rules (error for interactive rules).
    2. Add `--max-warnings 0` to the lint script in `package.json`.
    3. Type `useTranslation`'s `t` key parameter as a recursive dot-path union of `typeof enTranslations`. Add a type test in `useTranslation.test.ts`.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test src/hooks/useTranslation.test.ts }</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Extract remaining strings and deduplicate en.json</name>
  <files>src/a11y/en.json, src/utils/flowUtils.ts, src/components/editor/YamlEditor.tsx, src/components/settings/SettingsModal.tsx, src/components/projects/ProjectManager.tsx, src/components/studio/BatchMinerModal.tsx, src/components/projects/ExportImportPanel.tsx, src/components/shared/AgentSelector.tsx, src/components/ui/IconButton.tsx</files>
  <action>
    1. In `src/a11y/en.json`, dedupe repetitive values to `common.*` (`save`, `copied`, `clear`, `renameFlow`, `stepsCount`, `aiCopilot`, `yamlEditor`, `proqa`, `createProject`). Remove test fixture key.
    2. Extract `flowUtils.ts` categories into `en.json` (`common.flowCategories.*`).
    3. Extract `YamlEditor.tsx` `AUTOCOMPLETE_DESCRIPTIONS` into `en.json` (`editor.autocomplete.*`).
    4. Extract `SettingsModal.tsx` skill list into `en.json` (`settings.skills.*`).
    5. Extract straggling aria-labels/placeholders in `BatchMinerModal`, `ExportImportPanel`, `AgentSelector`, `ProjectManager`.
    6. In `IconButton.tsx`, add `aria-label={label}`.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rebuild a11yTextGuard test with full coverage</name>
  <files>src/a11y/a11yTextGuard.test.ts</files>
  <action>
    1. Update `src/a11y/a11yTextGuard.test.ts` to scan:
       - JSX text nodes (supporting colons, parentheses, ampersands, slashes, unicode)
       - Attributes: `aria-label`, `title`, `placeholder`, `alt`
       - String literals in JSX expression blocks
       - All directories under `src/` (including `src/utils/`, `src/lib/`, `src/hooks/`)
    2. Ensure allowable schema literals (e.g. YAML keywords, status IDs) are tightly constrained.
    3. Run test and verify 100% pass across all files.
  </action>
  <verify>
    <automated>pnpm test src/a11y/a11yTextGuard.test.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 4: Full project verification gate</name>
  <files></files>
  <action>
    1. Run `pnpm lint` (with `--max-warnings 0`).
    2. Run `pnpm test` (full vitest test suite).
    3. Run `pnpm exec vite build` (all client & electron bundles).
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }; if ($?) { pnpm exec vite build }</automated>
  </verify>
</task>

</tasks>

<output>
Create `.planning/phases/03-hardening/03-03-SUMMARY.md` on completion.
</output>
