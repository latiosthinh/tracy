---
phase: 12-qa-domain-skills-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/skills/builtins/authSessionSkill.ts
  - src/lib/skills/builtins/formValidationSkill.ts
  - src/lib/skills/builtins/dataTablesSkill.ts
  - src/lib/skills/builtins/shadowDomModalsSkill.ts
  - src/lib/skills/builtins/index.ts
  - src/lib/skills/builtins/builtins.test.ts
autonomous: true
requirements:
  - DOMAIN-01
  - DOMAIN-02
  - DOMAIN-03
  - DOMAIN-04

must_haves:
  truths:
    - "Auth & session edge cases skill (DOMAIN-01) provides specialized heuristics for login, MFA split inputs, cookie banners, and session timeouts"
    - "Dynamic form & validation handling skill (DOMAIN-02) provides heuristics for blur/tab triggers, custom selects/datepickers, and aria-invalid error state checks"
    - "Data tables & pagination skill (DOMAIN-03) provides scoped row/column locators, cell actions, and pagination navigation heuristics"
    - "Shadow DOM & modal overlay skill (DOMAIN-04) provides cross-boundary locators, open shadow roots, iframe boundaries, and animated modal backdrop wait strategies"
    - "All 4 domain skills validate against skillDefinitionSchema and are exported from builtins catalog"
    - "Unit test suite builtins.test.ts verifies schema compliance, prompt injection contents, and tool parameter validity"
  artifacts:
    - path: "src/lib/skills/builtins/authSessionSkill.ts"
      provides: "Auth and Session edge cases skill definition (auth-resilience)"
      contains: "export const authSessionSkill"
    - path: "src/lib/skills/builtins/formValidationSkill.ts"
      provides: "Dynamic form validation skill definition (form-validation)"
      contains: "export const formValidationSkill"
    - path: "src/lib/skills/builtins/dataTablesSkill.ts"
      provides: "Data tables and pagination skill definition (table-pagination)"
      contains: "export const dataTablesSkill"
    - path: "src/lib/skills/builtins/shadowDomModalsSkill.ts"
      provides: "Shadow DOM and modal overlays skill definition (shadow-dom-modal)"
      contains: "export const shadowDomModalsSkill"
    - path: "src/lib/skills/builtins/index.ts"
      provides: "Builtins barrel exporting BUILTIN_SKILLS and individual skills"
      exports: ["authSessionSkill", "formValidationSkill", "dataTablesSkill", "shadowDomModalsSkill", "BUILTIN_SKILLS", "SKILL_PRESETS"]
    - path: "src/lib/skills/builtins/builtins.test.ts"
      provides: "Unit test suite for built-in QA domain skills catalog"
      contains: "describe('Built-in QA Domain Skills Catalog'"
  key_links:
    - from: "src/lib/skills/builtins/index.ts"
      to: "src/lib/skills/builtins/authSessionSkill.ts"
      via: "import and export"
      pattern: "export \\* from '\\./authSessionSkill'"
    - from: "src/lib/skills/builtins/builtins.test.ts"
      to: "src/lib/skills/schema.ts"
      via: "validateSkillDefinition"
      pattern: "validateSkillDefinition\\("
---

<objective>
Implement the 4 built-in QA domain skills modules (`authSessionSkill.ts`, `formValidationSkill.ts`, `dataTablesSkill.ts`, `shadowDomModalsSkill.ts`), export them alongside presets in `src/lib/skills/builtins/index.ts`, and add comprehensive unit test suite in `builtins.test.ts`.

Purpose: Deliver battle-tested prompt engineering, locator heuristics, and tool definitions for complex E2E test scenarios (Auth/MFA, Forms, Data Tables, Shadow DOM/Modals).
Output: 4 skill files, updated builtins index, and comprehensive unit tests passing `pnpm test` and `pnpm lint`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/12-qa-domain-skills-catalog/12-CONTEXT.md
@src/types/skills.ts
@src/lib/skills/schema.ts
@src/lib/skills/builtins/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author 4 Modular QA Domain Skills in builtins/</name>
  <files>
    src/lib/skills/builtins/authSessionSkill.ts,
    src/lib/skills/builtins/formValidationSkill.ts,
    src/lib/skills/builtins/dataTablesSkill.ts,
    src/lib/skills/builtins/shadowDomModalsSkill.ts
  </files>
  <behavior>
    - authSessionSkill: id 'auth-resilience', domain 'auth', covers password fields, 2FA/MFA split inputs, cookie/consent banners, redirect delays, sensitive credential masking, and session timeout waits (DOMAIN-01).
    - formValidationSkill: id 'form-validation', domain 'forms', covers blur/tab triggers, custom dropdowns/datepickers, input masks, dirty states, aria-invalid checks, and inline error containers (DOMAIN-02).
    - dataTablesSkill: id 'table-pagination', domain 'tables', covers table row/column scoping, loading skeleton waits, sorting header triggers, pagination controls, and cell action locators (DOMAIN-03).
    - shadowDomModalsSkill: id 'shadow-dom-modal', domain 'shadow-dom', covers open shadow root traversal, iframe boundary handling, modal focus traps, backdrop dismissal, and CSS animation/transition waits (DOMAIN-04).
    - All 4 skills define valid tools / parameters if applicable and conform strictly to SkillDefinition type and skillDefinitionSchema.
  </behavior>
  <action>
    Create the four dedicated skill definition files in `src/lib/skills/builtins/`:
    1. `src/lib/skills/builtins/authSessionSkill.ts`: Export `authSessionSkill: SkillDefinition`. Include rich prompt rules for 2FA digit inputs (`input[autocomplete="one-time-code"]`, `input[aria-label*="digit"]`), cookie acceptance banner locators (`button:has-text("Accept")`, `[aria-label*="cookie"]`), password masking, and redirect wait assertions.
    2. `src/lib/skills/builtins/formValidationSkill.ts`: Export `formValidationSkill: SkillDefinition`. Include rich prompt rules for triggering field blur/dirty states (`press: Tab`), checking `aria-invalid="true"`, verifying field error message containers (`[role="alert"]`, `.error-message`), custom select/combobox interaction (`[role="combobox"]`, `[role="option"]`), and datepicker inputs.
    3. `src/lib/skills/builtins/dataTablesSkill.ts`: Export `dataTablesSkill: SkillDefinition`. Include rich prompt rules for scoping row actions (`tr:has-text(...) >> button`), column header sorting clicks, pagination next/prev page controls, skeleton loading spinners (`waitFor` disappearance), and deterministic cell assertions.
    4. `src/lib/skills/builtins/shadowDomModalsSkill.ts`: Export `shadowDomModalsSkill: SkillDefinition`. Include rich prompt rules for piercing open shadow roots (`pierce/` or direct CSS penetrating selectors), iframe context switching, modal backdrop dismissal, focus trap navigation, and waiting for dialog open/close transition animations (`waitFor: [role="dialog"][data-state="open"]` or visible check).
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>All four skill definition files exist with rich prompt instructions, correct IDs, valid domain categories, and export types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Built-in Index & Add Built-in Skills Catalog Test Suite</name>
  <files>
    src/lib/skills/builtins/index.ts,
    src/lib/skills/builtins/builtins.test.ts
  </files>
  <behavior>
    - BUILTIN_SKILLS in index.ts aggregates generic-qa and the 4 modular domain skills.
    - All 4 skills and presets are re-exported from index.ts.
    - builtins.test.ts validates every built-in skill with `validateSkillDefinition` (no schema errors).
    - builtins.test.ts verifies each skill contains essential domain rules (auth, forms, tables, shadow-dom).
    - builtins.test.ts verifies SKILL_PRESETS correctly map to valid skill IDs.
  </behavior>
  <action>
    1. Update `src/lib/skills/builtins/index.ts` to import `authSessionSkill`, `formValidationSkill`, `dataTablesSkill`, and `shadowDomModalsSkill` from their modular files. Re-export them individually as named exports. Ensure `BUILTIN_SKILLS` and `SKILL_PRESETS` maintain compatibility with existing IDs (`auth-resilience`, `form-validation`, `table-pagination`, `shadow-dom-modal`, `generic-qa`).
    2. Create `src/lib/skills/builtins/builtins.test.ts` to unit test:
       - Every skill in `BUILTIN_SKILLS` passes `validateSkillDefinition(skill)` with `valid: true` and 0 errors.
       - `authSessionSkill` includes heuristics for MFA, cookies, and password masking.
       - `formValidationSkill` includes heuristics for aria-invalid, blur/tab, and error containers.
       - `dataTablesSkill` includes heuristics for row scoping, sorting, and pagination.
       - `shadowDomModalsSkill` includes heuristics for shadow root penetration, dialogs, and animations.
       - `SKILL_PRESETS` only references existing skill IDs in `BUILTIN_SKILLS`.
       - Run full test suite to ensure `registry.test.ts` and `agentStore.test.ts` continue to pass without regression.
  </action>
  <verify>
    <automated>pnpm test src/lib/skills</automated>
  </verify>
  <done>`index.ts` cleanly exports the modular skills, `builtins.test.ts` verifies all schema and prompt requirements, and all skill tests pass green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| System Prompt Injection | Static prompt engineering text injected into LLM context window |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering / Information Disclosure | `authSessionSkill.ts` | mitigate | Skill prompt explicitly mandates credential masking and warns LLM against hardcoding raw secrets/tokens in test steps |
| T-12-02 | Denial of Service | `builtins/index.ts` | mitigate | Skills are validated via Zod schema before registration to avoid malformed prompt/tool structures causing infinite loops |
</threat_model>

<verification>
Run `pnpm test` and `pnpm lint` to verify complete test pass and lint cleanliness.
</verification>

<success_criteria>
- `authSessionSkill.ts`, `formValidationSkill.ts`, `dataTablesSkill.ts`, and `shadowDomModalsSkill.ts` implemented with high-quality domain testing heuristics
- `src/lib/skills/builtins/index.ts` re-exports all modular skills and standard presets
- `src/lib/skills/builtins/builtins.test.ts` provides 100% schema validation and prompt coverage
- `pnpm test` and `pnpm lint` pass cleanly with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-qa-domain-skills-catalog/12-01-SUMMARY.md`
</output>
