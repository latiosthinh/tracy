---
phase: 11-tool-calling-and-self-healing
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - electron/ipc/aiProvider.ts
  - electron/ipc/fileSystem.ts
  - electron/preload.ts
  - electron/preload.test.ts
  - src/lib/ipc.ts
  - electron/aiProvider.test.ts
autonomous: true
requirements:
  - TOOL-02

must_haves:
  truths:
    - "executeAgentToolLoop executes multi-turn tool execution up to a strict cap of 5 turns"
    - "When a proposed selector fails or is ambiguous, diagnostic error payload guides LLM to self-correct"
    - "Streaming agent_tool_trace events are emitted via WebContents sender on every turn"
    - "agent_tool_trace is whitelisted in electron/preload.ts and accessible via tracyApi.onAgentToolTrace"
    - "Loop detector stops repetitive identical failing tool calls"
  artifacts:
    - path: "electron/ipc/aiProvider.ts"
      provides: "Self-healing multi-turn agent tool execution loop"
      exports: ["executeAgentToolLoop"]
    - path: "electron/preload.ts"
      provides: "Whitelisted agent_tool_trace IPC channel"
      contains: "agent_tool_trace"
    - path: "src/lib/ipc.ts"
      provides: "tracyApi.onAgentToolTrace method and types"
      contains: "onAgentToolTrace"
    - path: "electron/preload.test.ts"
      provides: "Updated test covering agent_tool_trace on-channel whitelist"
      contains: "agent_tool_trace"
  key_links:
    - from: "electron/ipc/fileSystem.ts"
      to: "electron/ipc/aiProvider.ts"
      via: "calls executeAgentToolLoop during flow generation"
      pattern: "executeAgentToolLoop"
    - from: "src/lib/ipc.ts"
      to: "electron/preload.ts"
      via: "listens to agent_tool_trace channel"
      pattern: "agent_tool_trace"
---

<objective>
Implement the self-healing multi-turn agent tool execution loop in `electron/ipc/aiProvider.ts`, wire it to webview DOM probe tools, enforce hard 5-turn iteration guards, and stream real-time trace events over IPC to the renderer.

Purpose: Allow LLM agents to iteratively inspect and pre-validate selectors against live webviews, self-repairing broken or ambiguous selectors before finalizing YAML test flows (TOOL-02).
Output: Working `executeAgentToolLoop`, IPC whitelist entry and test update, `tracyApi.onAgentToolTrace` listener, and comprehensive unit tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-tool-calling-and-self-healing/11-CONTEXT.md
@.planning/phases/11-tool-calling-and-self-healing/11-01-PLAN.md
@electron/ipc/aiProvider.ts
@electron/ipc/fileSystem.ts
@electron/ipc/webviewManager.ts
@electron/preload.ts
@src/lib/ipc.ts
@electron/aiProvider.test.ts
@electron/preload.test.ts
</context>

<interfaces>
From src/types/skills.ts:
```typescript
export interface AgentToolTraceEvent {
  turn: number;
  thought?: string;
  toolCall?: {
    id?: string;
    name: string;
    arguments: Record<string, unknown>;
  };
  toolResult?: {
    valid: boolean;
    matchCount: number;
    details?: unknown;
    error?: string;
  };
  timestamp: string;
}
```

From electron/ipc/webviewManager.ts:
```typescript
export async function probeSelectorInWebview(
  view: WebContentsView,
  payload: SelectorValidationPayload
): Promise<SelectorValidationResult>;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose agent_tool_trace IPC channel in preload and tracyApi</name>
  <files>electron/preload.ts, electron/preload.test.ts, src/lib/ipc.ts</files>
  <behavior>
    - Add `agent_tool_trace` to `ALLOWED_ON_CHANNELS` in `electron/preload.ts`
    - Update `electron/preload.test.ts` to expect 6 on-channels including `agent_tool_trace`
    - Add `onAgentToolTrace(callback: (payload: AgentToolTraceEvent) => void): Promise<UnlistenFn>` to `tracyApi` in `src/lib/ipc.ts`
  </behavior>
  <action>
    1. In `electron/preload.ts`, add `'agent_tool_trace'` to `ALLOWED_ON_CHANNELS`.
    2. In `electron/preload.test.ts`, update `ALLOWED_ON_CHANNELS` and expected count from 5 to 6.
    3. In `src/lib/ipc.ts`, export `onAgentToolTrace` method under `tracyApi` with `isElectronEnv()` guard returning no-op unlisten function.
  </action>
  <verify>
    <automated>pnpm test electron/preload.test.ts</automated>
  </verify>
  <done>IPC channel whitelisted and tested, wrapper exported in `tracyApi`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement executeAgentToolLoop with bounded self-healing loop and trace streaming</name>
  <files>electron/ipc/aiProvider.ts, electron/ipc/fileSystem.ts, electron/aiProvider.test.ts</files>
  <behavior>
    - `executeAgentToolLoop` executes multi-turn tool calling with max 5 turns
    - Dispatches tool invocations (`validate_selector`, `find_elements_by_text`, `inspect_element`) against webview or tool handler
    - Returns diagnostic payload on failed or ambiguous selectors (`{ valid: false, matchCount: 0, reason: "Element not found" }` or `{ valid: false, matchCount: 3, reason: "Ambiguous selector matches 3 elements. Provide more specific testId, text, or parent container" }`)
    - Detects repetitive identical tool calls to prevent infinite loops
    - Emits `agent_tool_trace` event via `onTrace` callback / IPC sender on each turn
    - Integrates with `run_agent_cli_stream` in `electron/ipc/fileSystem.ts` when projectId is provided
  </behavior>
  <action>
    1. In `electron/ipc/aiProvider.ts`, implement `executeAgentToolLoop({ provider, prompt, systemInstruction, projectId, tools, onTrace, onChunk, maxTurns = 5 })`:
       - Run loop up to `maxTurns` (default 5 per D-02).
       - Maintain conversation history across turns.
       - Parse tool calls from model response. If no tool calls, return final YAML text.
       - If tool calls present:
         - Record trace event and emit via `onTrace({ turn, thought, toolCall, timestamp })`.
         - Execute tool via webview probe (`validateDomSelector` or `probeSelectorInWebview`) or fallback query.
         - If selector probe returns `matchCount === 0` or `matchCount > 1`, format diagnostic feedback payload containing matches, stability score, and suggestions for self-correction.
         - Loop guard: track past tool call signatures `[name, JSON.stringify(args)]`. If repeated identically 2+ times with failing result, append prompt warning forcing alternative strategy.
         - Emit trace event with `toolResult`.
         - Format tool result into provider's message history format and invoke next turn.
       - If loop reaches `maxTurns`, request final answer with all accumulated context.
    2. In `electron/ipc/fileSystem.ts`, update `run_agent_cli_stream` handler to accept optional `projectId` and invoke `executeAgentToolLoop` with streaming traces to `event.sender.send('agent_tool_trace', tracePayload)`.
    3. In `electron/aiProvider.test.ts`, add unit tests verifying:
       - Multi-turn loop correctly invokes tool handler and receives feedback
       - Self-correction diagnostic triggers when selector returns 0 matches
       - Max turn limit of 5 is strictly enforced
       - Repetitive loop detection stops runaway identical tool calls
       - Trace events are emitted for every turn with accurate timestamps and payloads
  </action>
  <verify>
    <automated>pnpm test electron/aiProvider.test.ts</automated>
  </verify>
  <done>Self-healing agent loop passes all unit tests, enforces 5-turn bounds, and streams traces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer ← Main Process IPC | Streaming trace events sent from Electron main process to renderer |
| LLM Tool Execution Loop | Uncontrolled loop execution consuming API tokens and CPU time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Denial of Service | `electron/ipc/aiProvider.ts` | mitigate | Hard-cap turns at 5 iterations maximum; loop detector breaks infinite repeat loops |
| T-11-05 | Information Disclosure | `electron/ipc/aiProvider.ts` | mitigate | Redact sensitive strings (API keys, password values in tool arguments) from trace events emitted over IPC |
| T-11-06 | Elevation of Privilege | `electron/preload.ts` | mitigate | Explicitly whitelist `agent_tool_trace` in `ALLOWED_ON_CHANNELS` without allowing wildcard channel listeners |
</threat_model>

<verification>
```bash
pnpm lint
pnpm test
```
</verification>

<success_criteria>
Self-healing tool loop executes multi-turn tool calls with live DOM feedback, enforces 5-turn cap, emits redacted traces, and passes full lint and test suites.
</success_criteria>

<output>
After completion, create `.planning/phases/11-tool-calling-and-self-healing/11-02-SUMMARY.md`
</output>
