---
phase: 09-skills-runtime-and-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/types/skills.ts
  - src/types/index.ts
  - src/lib/skills/schema.ts
  - src/lib/skills/serializer.ts
  - src/lib/skills/schema.test.ts
  - src/lib/skills/serializer.test.ts
autonomous: true
requirements:
  - SKILL-01

must_haves:
  truths:
    - "Developers can define agent skills with system prompt modifiers, parameter schemas, and tool definitions matching Zod contract"
    - "Skills can be serialized to and parsed from .skill.json and .skill.yaml formats with validation errors reported"
  artifacts:
    - path: "src/types/skills.ts"
      provides: "TypeScript type contracts for SkillDefinition, ToolDefinition, SkillDomain, SkillPreset"
      contains: "export interface SkillDefinition"
    - path: "src/lib/skills/schema.ts"
      provides: "Zod runtime schemas and validation methods for skills and tools"
      contains: "export const skillDefinitionSchema"
    - path: "src/lib/skills/serializer.ts"
      provides: "JSON and YAML serializer / parser functions with format auto-detection"
      exports: ["serializeSkill", "parseSkill"]
  key_links:
    - from: "src/lib/skills/serializer.ts"
      to: "src/lib/skills/schema.ts"
      via: "parse with Zod schema"
      pattern: "skillDefinitionSchema\\.parse|safeParse"
---

<objective>
Install `zod` runtime dependencies, declare core TypeScript contracts and Zod validation schemas for declarative Agent Skills, and implement bi-directional serialization (`.skill.json` and `.skill.yaml`).

Purpose: Deliver the foundational type system and declarative schema (SKILL-01) required for extensible agent capabilities, tool calling definitions, and custom skill persistence across Tracy.
Output: `src/types/skills.ts`, `src/lib/skills/schema.ts`, `src/lib/skills/serializer.ts`, and full unit test coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/09-skills-runtime-and-registry/09-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install Zod and define Skill TypeScript and Zod Schema Contracts</name>
  <files>package.json, src/types/skills.ts, src/types/index.ts, src/lib/skills/schema.ts, src/lib/skills/schema.test.ts</files>
  <behavior>
    - Test valid skill definition schema validation with id, name, description, version, domain ('auth' | 'forms' | 'tables' | 'shadow-dom' | 'generic'), systemPromptInjection, tools list, parameters schema, and tags
    - Test validation fails when required fields (id, name, description) are missing or invalid
    - Test validation handles optional tools, prompt modifiers, and tags gracefully
    - Test tool definition schema matches function/tool call structure (name, description, parameters JSON schema / Zod shape)
  </behavior>
  <action>
    1. Install `zod` (`pnpm add zod@^3.24.2`) and `zod-to-json-schema` (`pnpm add zod-to-json-schema@^3.24.3`).
    2. Create `src/types/skills.ts` declaring types: `SkillDomain`, `ToolDefinition`, `SkillParameterDef`, `SkillDefinition`, `SkillPreset`, `SkillValidationResult`. Export from `src/types/index.ts`.
    3. Create `src/lib/skills/schema.ts` defining Zod schemas: `toolDefinitionSchema`, `skillParameterSchema`, `skillDefinitionSchema`, and validation helper `validateSkillDefinition(input: unknown): SkillValidationResult`.
    4. Write comprehensive unit tests in `src/lib/skills/schema.test.ts`.
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/skills/schema.test.ts</automated>
  </verify>
  <done>All Zod schema validation tests pass and TypeScript types are exported cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Declarative Skill Serializer & Parser (.skill.json & .skill.yaml)</name>
  <files>src/lib/skills/serializer.ts, src/lib/skills/serializer.test.ts</files>
  <behavior>
    - Test serializing a SkillDefinition to JSON string and YAML string
    - Test parsing valid `.skill.json` and `.skill.yaml` strings back into validated SkillDefinition objects
    - Test error handling when parsing malformed JSON/YAML or invalid schema payloads with descriptive validation errors
    - Test format auto-detection by content or explicit format parameter ('json' | 'yaml')
  </behavior>
  <action>
    1. Create `src/lib/skills/serializer.ts` using native `JSON` and `js-yaml` (`yaml.dump`, `yaml.load`).
    2. Implement `serializeSkill(skill: SkillDefinition, format: 'json' | 'yaml'): string`.
    3. Implement `parseSkill(content: string, format?: 'json' | 'yaml'): { skill: SkillDefinition | null; errors?: string[] }`. Auto-detect YAML vs JSON if format not specified.
    4. Write unit tests in `src/lib/skills/serializer.test.ts` covering serialization round-trips, format detection, and corrupted payload validation errors.
  </action>
  <verify>
    <automated>pnpm vitest run src/lib/skills/serializer.test.ts</automated>
  </verify>
  <done>Skill serializer and parser round-trip flawlessly for both JSON and YAML with unit tests passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system / User input -> Skill Parser | Custom skill files from disk are untrusted input crossing into runtime memory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering / Injection | `src/lib/skills/serializer.ts` (`yaml.load`) | mitigate | Use `yaml.load` with `JSON_SCHEMA` or safe object parsing to prevent arbitrary code execution during YAML deserialization. |
| T-09-02 | Input Validation | `src/lib/skills/schema.ts` | mitigate | Strictly validate all loaded skill objects against Zod `skillDefinitionSchema` before passing to store or AI providers. |
</threat_model>

<verification>
Run lint and tests:
`pnpm lint && pnpm test src/lib/skills/`
</verification>

<success_criteria>
1. `pnpm typecheck` compiles with zero errors.
2. Unit tests in `src/lib/skills/schema.test.ts` and `src/lib/skills/serializer.test.ts` pass 100%.
3. Fulfills SKILL-01 requirement.
</success_criteria>

<output>
After completion, create `.planning/phases/09-skills-runtime-and-registry/09-01-SUMMARY.md`
</output>
