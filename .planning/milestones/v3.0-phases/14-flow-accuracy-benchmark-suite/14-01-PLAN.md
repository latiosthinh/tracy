---
phase: 14-flow-accuracy-benchmark-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/fixtures/eval/auth-flow.html
  - src/test/fixtures/eval/complex-form.html
  - src/test/fixtures/eval/data-table.html
  - src/test/fixtures/eval/modal-shadow.html
  - src/test/fixtures/eval/ground-truth/auth-flow.yaml
  - src/test/fixtures/eval/ground-truth/complex-form.yaml
  - src/test/fixtures/eval/ground-truth/data-table.yaml
  - src/test/fixtures/eval/ground-truth/modal-shadow.yaml
  - src/test/fixtures/eval/fixtures.test.ts
autonomous: true
requirements:
  - EVAL-01
must_haves:
  truths:
    - "Static HTML fixtures exist for auth, forms, data tables, and shadow DOM / modals"
    - "Canonical ground-truth YAML flows specify correct action steps, selectors, inputs, and waits for each fixture"
    - "Fixture integrity test verifies all HTML files load into jsdom and all YAML reference files conform to flow schema"
  artifacts:
    - path: "src/test/fixtures/eval/auth-flow.html"
      provides: "Deterministic HTML fixture for authentication and 2FA"
    - path: "src/test/fixtures/eval/complex-form.html"
      provides: "Deterministic HTML fixture for validation and custom inputs"
    - path: "src/test/fixtures/eval/data-table.html"
      provides: "Deterministic HTML fixture for tables, row actions, and pagination"
    - path: "src/test/fixtures/eval/modal-shadow.html"
      provides: "Deterministic HTML fixture for modals and shadow DOM roots"
    - path: "src/test/fixtures/eval/ground-truth/auth-flow.yaml"
      provides: "Canonical reference YAML flow for auth scenario"
    - path: "src/test/fixtures/eval/ground-truth/complex-form.yaml"
      provides: "Canonical reference YAML flow for form scenario"
    - path: "src/test/fixtures/eval/ground-truth/data-table.yaml"
      provides: "Canonical reference YAML flow for table scenario"
    - path: "src/test/fixtures/eval/ground-truth/modal-shadow.yaml"
      provides: "Canonical reference YAML flow for modal and shadow DOM scenario"
    - path: "src/test/fixtures/eval/fixtures.test.ts"
      provides: "Vitest verification suite for fixture validity"
  key_links:
    - from: "src/test/fixtures/eval/fixtures.test.ts"
      to: "src/test/fixtures/eval/ground-truth/*.yaml"
      via: "js-yaml parsing and schema validation"
      pattern: "yaml\\.load"
---

<objective>
Build deterministic static HTML benchmark fixtures and canonical ground-truth YAML test flows covering all 4 core QA domains: Auth with 2FA/Cookies, Complex Dynamic Forms, Data Tables with Pagination, and Shadow DOM with Animated Modals.

Purpose: Provide reproducible, zero-network test targets for automated accuracy scoring of AI-generated YAML flows.
Output: 4 HTML fixtures, 4 ground-truth YAML flows, and a Vitest fixture integrity test suite.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-flow-accuracy-benchmark-suite/14-CONTEXT.md
@docs/FLOW_SCHEMA.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create deterministic HTML benchmark fixtures</name>
  <files>
    src/test/fixtures/eval/auth-flow.html,
    src/test/fixtures/eval/complex-form.html,
    src/test/fixtures/eval/data-table.html,
    src/test/fixtures/eval/modal-shadow.html
  </files>
  <behavior>
    - auth-flow.html includes: cookie banner `#cookie-banner`, accept button `button[data-testid="accept-cookies"]`, login form `#login-form`, email input `input[name="email"]`, password input `input[name="password"]`, submit button `button[type="submit"]`, MFA code container `#mfa-container` with 6 split digit inputs `input[data-testid^="mfa-digit-"]`.
    - complex-form.html includes: form `#survey-form`, custom combobox select `div[role="combobox"][data-testid="country-select"]` with option listbox, datepicker input `input[type="date"][data-testid="birthdate-input"]`, submit button, and dynamic error messages with `aria-invalid="true"` and `div[role="alert"]`.
    - data-table.html includes: table container `#users-table-container`, sortable column headers `th button[data-testid^="sort-"]`, paginated rows `tr[data-testid^="user-row-"]`, row action buttons `button[data-testid^="edit-user-"]` / `button[data-testid^="delete-user-"]`, and pagination controls `button[data-testid="pagination-next"]` and page indicators.
    - modal-shadow.html includes: trigger button `button[data-testid="open-modal-btn"]`, modal backdrop `.modal-backdrop`, modal dialog `div[role="dialog"]`, close button `button[aria-label="Close"]`, and custom web component `<user-profile-badge>` with open shadow root containing internal action button.
  </behavior>
  <action>
    Create all 4 static HTML fixtures in `src/test/fixtures/eval/`:
    1. `auth-flow.html`: Complete markup with inline styles and interactive elements for cookie banner, credentials, submit, and MFA split inputs.
    2. `complex-form.html`: Complete markup for custom controls, validation triggers, `aria-invalid` attributes, and alert containers.
    3. `data-table.html`: Complete markup for tabular data, column sorting indicators, action buttons per row, and pagination.
    4. `modal-shadow.html`: Complete markup with standard modal dialog structure and template script setting up open shadow DOM custom element.
  </action>
  <verify>
    <automated>pnpm test src/test/fixtures/eval/fixtures.test.ts</automated>
  </verify>
  <done>All 4 HTML fixtures exist with well-formed markup, distinct testids, semantic ARIA roles, and required QA challenge elements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author canonical ground-truth YAML flows and fixture integrity test suite</name>
  <files>
    src/test/fixtures/eval/ground-truth/auth-flow.yaml,
    src/test/fixtures/eval/ground-truth/complex-form.yaml,
    src/test/fixtures/eval/ground-truth/data-table.yaml,
    src/test/fixtures/eval/ground-truth/modal-shadow.yaml,
    src/test/fixtures/eval/fixtures.test.ts
  </files>
  <behavior>
    - `auth-flow.yaml` specifies sequential steps: accept cookie banner, fill email, fill password, click submit, waitFor MFA prompt, fill MFA digits 1-6.
    - `complex-form.yaml` specifies sequential steps: click country select, select option, fill date, click submit, assertVisible error alerts.
    - `data-table.yaml` specifies sequential steps: click sort column, click row action button, click next page, assertVisible row on page 2.
    - `modal-shadow.yaml` specifies sequential steps: click open modal button, waitFor modal dialog visible, click action inside modal, click close modal.
    - `fixtures.test.ts` loads each HTML file via jsdom, parses each ground-truth YAML using `js-yaml`, and verifies that all selectors in ground-truth YAML match corresponding DOM nodes in the fixtures.
  </behavior>
  <action>
    1. Create canonical ground-truth reference YAML files in `src/test/fixtures/eval/ground-truth/` adhering strictly to `docs/FLOW_SCHEMA.md`.
    2. Create `src/test/fixtures/eval/fixtures.test.ts` using Vitest + jsdom:
       - Validate HTML syntax and essential DOM elements.
       - Validate YAML parsing and schema conformity for all 4 reference flows.
       - Execute locator query checks verifying that ground-truth selectors successfully resolve against their corresponding HTML fixture DOMs.
  </action>
  <verify>
    <automated>pnpm test src/test/fixtures/eval/fixtures.test.ts</automated>
  </verify>
  <done>All 4 ground-truth YAML flows validate against schema and all test assertions in `fixtures.test.ts` pass green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture loading -> jsdom DOM | Static HTML content loaded into test environment jsdom |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | `src/test/fixtures/eval/*.html` | mitigate | Use static, self-contained HTML files without external script CDNs or live remote resources |
| T-14-02 | Information Disclosure | `src/test/fixtures/eval/ground-truth/*.yaml` | mitigate | Use mock/fake credentials (`user@example.com`, `Password123!`) in test flows |
</threat_model>

<verification>
Run `pnpm test src/test/fixtures/eval/fixtures.test.ts` to confirm all fixtures and ground-truth flows are valid.
</verification>

<success_criteria>
- 4 HTML fixture files created in `src/test/fixtures/eval/`
- 4 ground-truth YAML flow files created in `src/test/fixtures/eval/ground-truth/`
- Fixture test passes with 100% selector resolution against fixture DOMs
</success_criteria>

<output>
After completion, create `.planning/phases/14-flow-accuracy-benchmark-suite/14-01-SUMMARY.md`
</output>
