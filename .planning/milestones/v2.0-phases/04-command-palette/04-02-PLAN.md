---
phase: 04-command-palette
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/hooks/useGlobalShortcuts.ts
  - src/hooks/useGlobalShortcuts.test.ts
  - src/components/shared/ShortcutsModal.tsx
  - src/components/layout/AppShell.tsx
  - src/a11y/en.json
autonomous: true
requirements:
  - PALETTE-02
  - PALETTE-03

must_haves:
  truths:
    - "Ctrl+Enter starts executing the active test flow from anywhere"
    - "Ctrl+Shift+P toggles pause/resume execution"
    - "Ctrl+1 through Ctrl+9 switches to the corresponding open project tab"
    - "Ctrl+Tab / Ctrl+Shift+Tab cycles open flow tabs forward/backward"
    - "Ctrl+S triggers auto-save and active flow YAML save"
    - "Pressing ? (when not in an input/textarea) opens the Shortcuts Cheatsheet modal"
    - "All text strings flow through en.json"
---

<objective>
Implement global keyboard shortcuts manager hook (`useGlobalShortcuts`), register runner/tab/save actions, and build the Shortcuts Cheatsheet modal (`ShortcutsModal.tsx`).
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build useGlobalShortcuts hook and unit tests</name>
  <files>src/hooks/useGlobalShortcuts.ts, src/hooks/useGlobalShortcuts.test.ts, src/components/layout/AppShell.tsx</files>
  <action>
    1. Create `src/hooks/useGlobalShortcuts.ts` registering document keydown handler:
       - `Ctrl+Enter` / `Cmd+Enter`: trigger `startExecution`
       - `Ctrl+Shift+P` / `Cmd+Shift+P`: trigger `pauseExecution`
       - `Ctrl+1..9`: select project tab by index
       - `Ctrl+Tab` / `Ctrl+Shift+Tab`: cycle active flow tab
       - `Ctrl+S`: save flow / trigger quick save feedback
       - `?` (when `!(e.target instanceof HTMLInputElement || e.target instanceof HTMLTextAreaElement)`): open shortcuts cheatsheet
    2. Write unit tests in `src/hooks/useGlobalShortcuts.test.ts`.
    3. Mount `useGlobalShortcuts` inside `AppShell.tsx`.
  </action>
  <verify>
    <automated>pnpm test src/hooks/useGlobalShortcuts.test.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Build ShortcutsModal cheatsheet component</name>
  <files>src/components/shared/ShortcutsModal.tsx, src/stores/uiStore.ts, src/a11y/en.json, src/components/layout/AppShell.tsx</files>
  <action>
    1. In `src/stores/uiStore.ts`, add `isShortcutsModalOpen` and `setShortcutsModalOpen`.
    2. Add `shortcuts` translations in `src/a11y/en.json`.
    3. Build `src/components/shared/ShortcutsModal.tsx` using `Modal.tsx` displaying categorized badges (Navigation, Execution, Studio, Palette).
    4. Connect open trigger in `AppShell.tsx` and Settings.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }</automated>
  </verify>
</task>

</tasks>

<output>
Create `.planning/phases/04-command-palette/04-02-SUMMARY.md` on completion.
</output>
