---
phase: 04-command-palette
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/uiStore.ts
  - src/a11y/en.json
  - src/components/shared/CommandPalette.tsx
  - src/components/layout/AppShell.tsx
  - src/components/shared/CommandPalette.test.tsx
autonomous: true
requirements:
  - PALETTE-01

must_haves:
  truths:
    - "Pressing Ctrl+K or Ctrl+P toggles the Command Palette modal open with immediate autofocus"
    - "Typing queries filters items across actions, flows, projects, and navigation categories"
    - "Up/Down arrows navigate items and Enter executes the selected command and closes palette"
    - "Escape closes palette and restores previous focus"
    - "All user-facing strings flow through en.json via useTranslation"
---

<objective>
Implement spotlight-style Command Palette (`Ctrl+K` / `Ctrl+P`) with category grouping, dynamic fuzzy/substring search, keyboard navigation, accessible combobox semantics, and unit tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add command palette state to uiStore and translation keys to en.json</name>
  <files>src/stores/uiStore.ts, src/a11y/en.json</files>
  <action>
    1. In `src/stores/uiStore.ts`, add `isCommandPaletteOpen: boolean` and `setCommandPaletteOpen: (open: boolean) => void`, `toggleCommandPalette: () => void`.
    2. In `src/a11y/en.json`, add `palette` domain with placeholder, categories (`actions`, `flows`, `projects`, `navigation`), and empty state strings.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build CommandPalette component and unit tests</name>
  <files>src/components/shared/CommandPalette.tsx, src/components/shared/CommandPalette.test.tsx, src/components/layout/AppShell.tsx</files>
  <action>
    1. Build `src/components/shared/CommandPalette.tsx` collecting commands from `useProjectStore`, `useExecutionStore`, and `useUiStore`.
    2. Support `ArrowDown`, `ArrowUp`, `Enter`, and `Escape` keyboard handling.
    3. Mount `<CommandPalette />` in `AppShell.tsx` and register `Ctrl+K` / `Ctrl+P` (and `Cmd+K` / `Cmd+P` on Mac).
    4. Write unit tests in `src/components/shared/CommandPalette.test.tsx` verifying render, filtering, selection, and keyboard execution.
  </action>
  <verify>
    <automated>pnpm test src/components/shared/CommandPalette.test.tsx</automated>
  </verify>
</task>

</tasks>

<output>
Create `.planning/phases/04-command-palette/04-01-SUMMARY.md` on completion.
</output>
