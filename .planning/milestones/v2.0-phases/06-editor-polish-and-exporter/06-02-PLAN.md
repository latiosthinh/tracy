---
phase: 06-editor-polish-and-exporter
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/editor/VisualStepEditor.tsx
  - src/stores/projectStore.ts
  - src/a11y/en.json
autonomous: true
requirements:
  - EDIT-02

must_haves:
  truths:
    - "Users can duplicate steps with one click (or Alt+Drag / Ctrl+D) in the VisualStepEditor"
    - "Users can multi-select steps (Shift+Click / Ctrl+Click) to bulk delete or re-order"
    - "Step blocks have visible drag handles and keyboard reorder accessibility"
    - "All text strings flow through en.json"
---

<objective>
Enhance `VisualStepEditor.tsx` and `projectStore.ts` with step duplication, multi-step selection for bulk deletion, and improved step block interaction.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add step duplication and bulk manipulation to projectStore</name>
  <files>src/stores/projectStore.ts, src/stores/projectStore.test.ts</files>
  <action>
    1. In `src/stores/projectStore.ts`, add `duplicateStep: (flowId: string, stepIndex: number) => void`, `bulkDeleteSteps: (flowId: string, stepIndices: number[]) => void`.
    2. Write unit tests in `src/stores/projectStore.test.ts` verifying duplicateStep inserts a cloned step with fresh id at `stepIndex + 1` and bulkDeleteSteps cleanly removes selected indices.
  </action>
  <verify>
    <automated>pnpm test src/stores/projectStore.test.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Update VisualStepEditor with multi-select and duplicate actions</name>
  <files>src/components/editor/VisualStepEditor.tsx, src/a11y/en.json</files>
  <action>
    1. In `src/components/editor/VisualStepEditor.tsx`, add a "Duplicate" button (`Copy` icon) on each step card.
    2. Add multi-selection state (`selectedStepIndices: number[]`), select-all checkbox, and bulk delete toolbar when 1+ steps are selected.
    3. Add translations in `src/a11y/en.json` for `editor.duplicateStep`, `editor.bulkDelete`, `editor.selectedSteps`.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }</automated>
  </verify>
</task>

</tasks>

<output>
Create `.planning/phases/06-editor-polish-and-exporter/06-02-SUMMARY.md` on completion.
</output>
