---
phase: 07-ai-recipes-and-diff-preview
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - src/components/ai/AiCopilot.tsx
  - src/a11y/en.json
autonomous: true
requirements:
  - AI-03

must_haves:
  truths:
    - "During streaming generation, AI Copilot shows live tokens/sec speed and token count metrics"
    - "Latency / duration is displayed once generation finishes"
    - "All text strings flow through en.json via useTranslation"
---

<objective>
Implement real-time generation speed telemetry (tokens/sec, token count, elapsed duration) in `AiCopilot.tsx`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Implement live generation telemetry in AiCopilot</name>
  <files>src/components/ai/AiCopilot.tsx, src/a11y/en.json</files>
  <action>
    1. In `src/components/ai/AiCopilot.tsx`, calculate generation metrics on chunk arrival:
       - Elapsed time (ms/sec)
       - Estimated tokens count (words / characters ratio ~ 4 chars/token)
       - Speed: tokens / second
    2. Render telemetry chip in the streaming box header (`Zap` / `Gauge` icon).
    3. Add translations in `src/a11y/en.json` under `copilot.telemetry.*`.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }</automated>
  </verify>
</task>

</tasks>

<output>
Create `.planning/phases/07-ai-recipes-and-diff-preview/07-03-SUMMARY.md` on completion.
</output>
