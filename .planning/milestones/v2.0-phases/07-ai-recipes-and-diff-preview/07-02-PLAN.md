---
phase: 07-ai-recipes-and-diff-preview
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/components/ai/AiDiffPreviewModal.tsx
  - src/components/ai/AiCopilot.tsx
  - src/a11y/en.json
autonomous: true
requirements:
  - AI-02

must_haves:
  truths:
    - "Generated AI YAML can be previewed side-by-side against active flow before applying"
    - "User can choose to Replace Entire Flow or Append Steps to End or Cancel"
    - "All text strings flow through en.json via useTranslation"
---

<objective>
Build `AiDiffPreviewModal.tsx` comparing active flow YAML vs AI-generated YAML with 1-click Replace or Append actions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Build AiDiffPreviewModal component</name>
  <files>src/components/ai/AiDiffPreviewModal.tsx, src/a11y/en.json</files>
  <action>
    1. Build `src/components/ai/AiDiffPreviewModal.tsx` using `Modal.tsx` and `computeLineDiff` from `src/utils/diffUtils.ts`.
    2. Show side-by-side diff with addition/deletion stats and three actions:
       - Replace Active Flow (`onReplace`)
       - Append Steps (`onAppend`)
       - Cancel (`onClose`)
    3. Add translations in `src/a11y/en.json` under `copilot.diffPreview.*`.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Integrate AiDiffPreviewModal into AiCopilot</name>
  <files>src/components/ai/AiCopilot.tsx</files>
  <action>
    1. In `src/components/ai/AiCopilot.tsx`, add a "Preview Diff Before Apply" button next to "Apply to Active Editor".
    2. Support appending steps (extracting steps from generated YAML and appending to active flow steps array).
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }</automated>
  </verify>
</task>

</tasks>

<output>
Create `.planning/phases/07-ai-recipes-and-diff-preview/07-02-SUMMARY.md` on completion.
</output>
