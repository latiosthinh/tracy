---
phase: 05-studio-layout-and-viewports
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/studio/RealBrowserView.tsx
  - src/components/studio/StudioToolbar.tsx
  - src/stores/uiStore.ts
  - src/a11y/en.json
autonomous: true
requirements:
  - LAYOUT-02

must_haves:
  truths:
    - "Users can enable realistic device bezel frames (iPhone/Mobile, iPad/Tablet, Laptop, Desktop) around the embedded browser"
    - "Users can toggle device orientation (Portrait vs. Landscape) for Mobile and Tablet presets"
    - "Bezel frames resize child webview bounds accurately to fit inside the simulated screen viewport"
    - "All text strings flow through en.json"
---

<objective>
Implement realistic device bezel frames and orientation rotation (portrait/landscape) for the embedded browser view in `RealBrowserView.tsx` and `StudioToolbar.tsx`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add device framing and orientation state</name>
  <files>src/stores/uiStore.ts, src/a11y/en.json</files>
  <action>
    1. In `src/stores/uiStore.ts`, add `deviceOrientation: 'portrait' | 'landscape'`, `showDeviceBezel: boolean`, `toggleDeviceOrientation: () => void`, `toggleDeviceBezel: () => void`.
    2. Add translations in `src/a11y/en.json` under `layout.deviceFrame`, `layout.rotateOrientation`, `layout.portrait`, `layout.landscape`.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Build device bezel frames in RealBrowserView and toolbar triggers</name>
  <files>src/components/studio/RealBrowserView.tsx, src/components/studio/StudioToolbar.tsx</files>
  <action>
    1. In `src/components/studio/StudioToolbar.tsx`, add buttons for orientation flip (rotate icon) and device frame toggle.
    2. In `src/components/studio/RealBrowserView.tsx`, render realistic styled bezels (rounded outer borders, speaker notch, home indicator) when `showDeviceBezel` is enabled.
    3. Swap width/height dimensions when `deviceOrientation === 'landscape'` for mobile (375x812 -> 812x375) and tablet (768x1024 -> 1024x768).
    4. Ensure `containerRef` wraps the inner screen area so `getBoundingClientRect()` accurately positions the native `WebContentsView` inside the frame.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }</automated>
  </verify>
</task>

</tasks>

<output>
Create `.planning/phases/05-studio-layout-and-viewports/05-02-SUMMARY.md` on completion.
</output>
