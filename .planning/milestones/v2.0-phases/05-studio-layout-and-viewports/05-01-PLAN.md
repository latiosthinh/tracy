---
phase: 05-studio-layout-and-viewports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/uiStore.ts
  - src/a11y/en.json
  - src/components/studio/StudioView.tsx
  - src/components/studio/StudioToolbar.tsx
autonomous: true
requirements:
  - LAYOUT-01

must_haves:
  truths:
    - "User can toggle studio layout between side-by-side (vertical) and top-and-bottom (horizontal) split"
    - "Layout orientation and divider position persist in localStorage via uiStore"
    - "Draggable divider functions smoothly in both horizontal and vertical orientations with accessible role='slider' and keyboard resizing"
    - "All text and tooltips flow through en.json via useTranslation"
---

<objective>
Implement horizontal vs. vertical studio split orientation with persistent state in `uiStore`, orientation toggle button in `StudioToolbar`, responsive resizer divider, and full keyboard resizing support.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add split orientation state to uiStore and translations</name>
  <files>src/stores/uiStore.ts, src/a11y/en.json</files>
  <action>
    1. In `src/stores/uiStore.ts`, add `splitOrientation: 'vertical' | 'horizontal'`, `sidePanelHeight: number`, `setSplitOrientation: (orientation: 'vertical' | 'horizontal') => void`, `toggleSplitOrientation: () => void`, `setSidePanelHeight: (height: number) => void`, persisting `splitOrientation` and `sidePanelHeight` to localStorage.
    2. In `src/a11y/en.json`, add translation keys for `layout.splitVertical`, `layout.splitHorizontal`, `layout.toggleSplit`.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Update StudioView and StudioToolbar with flexible split layout</name>
  <files>src/components/studio/StudioView.tsx, src/components/studio/StudioToolbar.tsx</files>
  <action>
    1. In `src/components/studio/StudioToolbar.tsx`, add a split orientation toggle button (`LayoutGrid` / `Columns` / `Rows` icons) with tooltip and `aria-label`.
    2. In `src/components/studio/StudioView.tsx`, conditionally render `flex-row` (vertical split) or `flex-col` (horizontal split).
    3. Update the divider drag handler and `role="slider"` attributes to support horizontal resizing (`clientY` delta and `ArrowUp`/`ArrowDown` keys) when `splitOrientation === 'horizontal'`.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }</automated>
  </verify>
</task>

</tasks>

<output>
Create `.planning/phases/05-studio-layout-and-viewports/05-01-SUMMARY.md` on completion.
</output>
