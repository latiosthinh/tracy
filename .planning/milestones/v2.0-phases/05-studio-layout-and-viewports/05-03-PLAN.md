---
phase: 05-studio-layout-and-viewports
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - electron/ipc/webviewManager.ts
  - electron/ipc/playwrightEngine.ts
  - src/lib/ipc.ts
  - src/components/studio/StudioToolbar.tsx
  - src/stores/uiStore.ts
  - src/a11y/en.json
autonomous: true
requirements:
  - LAYOUT-03

must_haves:
  truths:
    - "Users can toggle dark / light color-scheme emulation for the embedded webview browser from the studio toolbar"
    - "Toggling color-scheme sends IPC to emulate prefers-color-scheme in the active WebContentsView"
    - "All text strings flow through en.json"
---

<objective>
Implement dark/light color-scheme media emulation for the embedded browser view via Electron IPC and Playwright engine.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add color scheme emulation IPC handler and tracyApi wrapper</name>
  <files>electron/ipc/webviewManager.ts, electron/preload.ts, src/lib/ipc.ts</files>
  <action>
    1. In `electron/ipc/webviewManager.ts`, implement `emulate_media_theme` IPC handler: `entry.view.webContents.emulateMedia({ colorScheme: theme })`.
    2. Add `emulate_media_theme` to `ALLOWED_INVOKE_CHANNELS` in `electron/preload.ts` and `electron/preload.test.ts`.
    3. In `src/lib/ipc.ts`, add `emulateMediaTheme: async (projectId: string, theme: 'dark' | 'light' | 'no-preference'): Promise<void>`.
  </action>
  <verify>
    <automated>pnpm test electron/preload.test.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Add theme toggle in StudioToolbar and persist in uiStore</name>
  <files>src/stores/uiStore.ts, src/components/studio/StudioToolbar.tsx, src/a11y/en.json</files>
  <action>
    1. In `src/stores/uiStore.ts`, add `pageThemeEmulation: 'system' | 'dark' | 'light'`, `setPageThemeEmulation`.
    2. In `src/a11y/en.json`, add translations under `layout.pageThemeDark`, `layout.pageThemeLight`, `layout.pageThemeSystem`.
    3. In `src/components/studio/StudioToolbar.tsx`, add a Moon/Sun theme toggle button invoking `tracyApi.emulateMediaTheme`.
  </action>
  <verify>
    <automated>pnpm lint; if ($?) { pnpm test }</automated>
  </verify>
</task>

</tasks>

<output>
Create `.planning/phases/05-studio-layout-and-viewports/05-03-SUMMARY.md` on completion.
</output>
