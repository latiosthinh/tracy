/**
 * @deprecated Use `useUiStore` from '@/src/stores/uiStore' directly.
 * This file exists only for backward compatibility during migration.
 * All settings state and actions have been consolidated into uiStore.
 */
export { useUiStore as useSettingsStore } from '@/src/stores/uiStore';
